# Verify_Task4C_P35GTHead_1.1

2026-09-17 用户要求在最新扩充集合训练和测试p35/h0，并明确选择Task4-c原有小网络：76,738参数、32点、最近六邻居。本实验只有这一固定方案，不搜索、不重新采样。

## 固定数据

直接只读复用 `mainExp_Task4C_GTHeadCoverage_1.1` 执行r2的196,960训练、3,000验证、11,320测试。Channel分别100,720/1,500/5,740，TBL分别96,240/1,500/5,580。18个geometry/seeds/metadata文件SHA256及原完整数据审计SHA256写入配置，运行前后逐一核验。不复制或重建几何，不修改标签。原验证文件未扩充；原测试各5,000束后追加Channel740/TBL580束正类。132个GT每个30追加训练、10追加测试，仍为共享实例的局部评估。

## 冻结方法

| 版本 | 技术 | 代码 | 结果 |
|---|---|---|---|
| 1.1 / p35_h0 | 原32点、每条有效线作中心、播种位置最近六邻居；中心23＋方向72＋邻居逐特征均值23/最大值23；原neighbor_scale=100、neighbor_weight=0.5；signed-log→训练集有效线逐特征标准化→截断[-8,8] | `Task4C_LinePooling_4_3.line_fmt`、`FMT_V8_Search_2_1.select_features/task4_model`；新适配 `experiments/Task4C_P35GTHead_1_1.py` | 待执行 |

本版是字面意义的原p35/h0，不静默换成后续p35/n0_k06：后者的邻居倍率/权重为1，且没有signed-log和截断。几何数组已经按整束质心/最大半径归一化，保持原32点数组不再重采样。每束10–27有效线，补零标记不进入首层。

逐线141→128→128；有效线均值/最大值拼接256→128→64→2。76,738参数，全量训练；没有Raw坐标旁路或c156加宽残差块。Dropout0.15、AdamW学习率0.001、权重衰减0.0001、batch128、梯度范数裁剪5。ReduceLROnPlateau监测验证损失，patience15、factor0.5、min_lr1e-6。每次最多500轮、50轮早停；固定0.5阈值，以验证F1、随后平均精确率选择轮次。复用冻结FPS搜索的训练循环，只替换数据读取、原版编码和标准化，禁用全部增强。

三个预先固定种子96721/96722/96723，V100，最多三卡并发。完整保留每种子，不按测试选种子。现有c156扩充集结果只有96721，复用该预测；仅96721为同种子比较，不将c156单种子与p35三种子均值说成配对验证。旧c156三种子使用旧数据，不混入本轮。

## 运行前检查与结果

先CPU合成输入检查和配置核验，再远程18文件哈希审计；V10032真实训练束小拟合、参数数、无效线屏蔽、128批反向、CPU/GPU特征误差检查通过后才允许三次正式训练。检查只读训练数据。训练/验证/测试均在V100上编码，避免此前ASAP比较的CPU训练编码/GPU测试编码混用。

每个种子锁定轮次后才读取测试特征，保存全部验证和测试预测、不保存权重。独立复算全部预测、标签/行号/GT身份、模型选择和原数据哈希。报告完整测试F1/精确率/召回率/平均精确率，原10,000束子集F1，新增1,320正类头部召回及每GT10束正确/漏检数；报告每种子和均值/样本标准差、分流场及训练时间。新增子集只有正类，不以其F1替代主报告。

调度链：只读数据核验1CPU→V100工程预检1→V100正式训练数组3→独立结果核验1CPU，共6进程。独立实验目录，全部任务和失败登记；源GTHead数据、c156结果及其他任务不修改。

# Task4-c五倍训练数据的8³/12³卷积：Ablation_Task4C_BottomDensity_1.3

2026-09-16用户要求追加测试8³和12³ Conv3D。只新增这两个分辨率，各三个种子96611–96613，共六次训练。

## 冻结的数据和训练

全部使用已完成的1.2数据：Channel98,500、TBL94,500训练束，合计193,000训练/3,000验证/10,000测试。来源科学commit `a7643890d22c7faf63c1f716bb6222ba5271c5ff`；配置SHA256 `c92b892ed1db9e6e01d64f4eea7792e616e5ab6f0b4a7436b3b456739be44363`。直接复用原geometry.npy、seeds.npy和metadata.npz，核对全部文件哈希后建立只供读取的目录引用，不重新播种、不重新积分或划分。

只用相同的四通道体素化函数生成8³/12³缓存。每个模型仍为72,192参数：12/24/48卷积通道，固定2³输出池化，384→70→64→2分类头。两次最大池化后，8³/12³的空间大小分别为2³/3³，后续相同自适应池化得到2³；卷积核、归一化、dropout及参数量不变。稀疏float16缓存恢复为原体素输入。

保持1.2训练器：V100、batch128、lr0.001、dropout0.15、weight decay0.0001、最多500轮、验证损失平台降学习率；验证F1/AP选最佳轮次、50轮不改善早停，固定0.5阈值测试一次。每轮完整遍历训练集。FMT/Conv16/Conv24沿用1.2已核验结果，不重跑；1.1的8³结果来自更小训练集，不能替代本轮8³。

本次沿用已经使用过的测试benchmark，不根据新增测试结果继续改参数；不宣称新的独立实例确认集。无checkpoint文件。

## 实现与验证

配置 `config/Ablation_Task4C_BottomDensity_1.3.json` 固定继承1.2，只覆盖版本、输出、来源和两种分辨率。入口 `experiments/Task4C_BottomDensity_1_3.py`；Slurm入口 `ibex_bash/task4c_bottom_density_1p3.sh`。复用原网络、体素化、训练、指标和预测复算代码；1.2源码和成绩冻结。

CPU/V100预检核对8³/12³体素独立参考、稀疏往返、网络前后向和参数量。依赖链：预检1进程→数据复用/哈希检查1进程→两流场体素缓存2进程→六次训练→汇总1进程，共11进程。失败自动阻止依赖项。六次训练均保存train/validation/test逐样本预测，汇总复算18份预测，记录参数量、训练时间、合并及分流场F1。Ibex编号登记于运行表，方法结论只写入实验日志。

科学commit `12a0ffa7fbc6f530c3bc3ce1248cbc8fc028e132` 已公开推送；Ibex51927009–51927013共11进程已提交，含六次训练。CPU/V100模型/体素检查和原18个数据文件哈希核对通过，数据数量193,000/3,000/10,000不变；六次训练等待编码结束。当前暂无新F1，启动证据为outputs/Ablation_Task4C_BottomDensity_1.3/startup_evidence.json。

2026-09-16最终完成：六次训练、18份预测及11进程22事件独立复算通过，原18个数据文件哈希不变，无模型文件。科学commit12a0ffa7；结果见paper_tables_tasks_3d及experiment_log的task4c-fivefold-conv8-conv12-final-20260916。本次请求完成，不自动追加调参。

# Task4-c 4.18：限制三维卷积参数量

版本`Ablation_Task4C_ConvCapacity_4.18`，2026-09-14用户要求重新训练Conv3D，参数量只能为4.14 FMT方法的90%–95%。基准为4.14的88,514个可训练参数，不是4.15/4.16新编码器模型。允许范围整数79,663–84,088；预先固定新结构83,918个参数，即94.8076%。旧219,602参数Conv和原FMT结果保留，不改名替换。只跑这一结构的三个种子，不根据测试选择宽度。

| 层 | 4.14 Conv | 4.18 Conv |
|---|---|---|
| 三层3³卷积通道 | 4→16→32→64 | 4→12→24→48 |
| 两次最大池化与最终平均池化 | 24³→12³→6³→2³ | 相同 |
| 展平后分类头 | 512→256→64→2 | 384→96→64→2 |
| 可训练参数 | 219,602 | 83,918 |

GroupNorm分组4、GELU、Dropout3d、LayerNorm及分类激活位置均保持原顺序。最后两个Linear之间仍没有激活，这是冻结原结构的选择，本次不顺便修改。输入为原4.14四通道24³体素，沿用原体素缓存的每一位，不重新量化、改变体素分辨率、重新采样或改标签。

数据固定原27,000训练、3,000验证、10,000测试；Channel/TBL合并训练。旧物理积分长度、尺度、空间块和GT保持。测试是已用过的4.14已知头区内空间块评测，不是新的独立确认。数据缓存通过哈希核验后以只读使用方式链接；源数据不写入。

完全复用4.14调用的4.13训练器：96611/96612/96613三种子，batch128、AdamW初始学习率0.001、weight decay0.0001、dropout0.15（卷积0.075），普通交叉熵、梯度裁剪5。每轮全27,000行无放回一次；验证损失平台15轮学习率减半、最低1e−6；最多500轮，验证F1@0.5优先、Average Precision平分时比较，50轮无最佳改善早停。模型选择锁写入后每个种子一次测试。阈值0.5为主，验证所选阈值为补充。原FMT和大Conv使用冻结4.14参考，不额外重训。

限定原Tesla V100-SXM2-32GB，4 CPU线程。报告每种子训练至最后验证耗时（包括已编码数据加载）、数据加载耗时、第一轮以后的每轮耗时中位数/均值、总轮数和选中轮数。总训练时间受早停和文件系统影响，参数更少不保证耗时按比例下降；不同结构不能仅用总时间断言每步加速。原4.14的实际记录作为历史参考。

代码入口`experiments/Task4C_ConvCapacity_4_18.py`集中结构、缓存核验、训练适配和正式预测复算；配置`config/Ablation_Task4C_ConvCapacity_4.18.json`；部署`ibex_bash/task4c_conv_capacity_4p18.sh`。旧训练器及网络源码不修改。仅内存最佳权重，不产生checkpoint。准备1项、训练3项、汇总及审计1项，共5个Slurm进程，完整登记。

本地内联检查：参数公式与实际数目、算子顺序、所有参数梯度、27组独立分类指标及三次真实工程小缓存训练通过。首份工程夹具要求每流场16个正类，但本地预检只有13/12个，改用每类8个、合计32个既有工程训练样本；生产配额不变。夹具跨集合复用只是训练接口检查，分数不作为科研结果。证据集中`outputs/Verify_Task4C_ConvCapacityCode_4.18/checks.json`与实际夹具配置，不新增临时test/verify代码文件。正式结果独立复算全部分组指标、验证选定epoch/阈值、逐轮训练覆盖和输入身份，并核对源码/配置/预测哈希。

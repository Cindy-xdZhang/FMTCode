# Task4-c：流场范围、网格分辨率与 ball query 的 h（2.4）

2026-09-19 用户确认：**Channel h = 0.003909492；TBL h = 0.117239990**。本文件是当前 h 定义，取代 2.2 的实际最小相邻间距和 2.3 的指定轴最小相邻间距；历史配置、统计与结果保留，不混用。

## 原始流场范围与分辨率

坐标范围读取原始 VTK 文件，单位保持原始坐标单位。分辨率按 **x × y × z 的格点数**记录，每个方向的区间数是格点数减一。下表端点显示到小数点后12位，计算使用文件中的完整坐标值。

| 流场 | 网格分辨率（格点数） | x 范围 | y 范围 | z 范围 |
|---|---|---|---|---|
| Channel | **256 × 192 × 256** | [0, 3.129320859909] | [0, 1.171961307526] | [−1, −0.003079524264] |
| TBL | **276 × 251 × 224** | [271.290008544922, 351.649993896484] | [164.139999389648, 193.449996948242] | [0.003578899894, 26.488000869751] |

文件来源：`C:/Users/xingdi/OneDrive - KAUST/WorkingInProcess/FLowVisAssets/flowData3D/channel_flow/channel.vtk`、同目录下 `tbl_flow/tbl.vtk`。Channel 文件 SHA-256 为 `c1e3c18d0e67b6eb33ea41cd49c3a4c55216838a0f4a42f778ef00aaba3d4564`；TBL 为 `cc82db1d3ca4e1964ec19dcd30df5a82c3c5635c16b8e0182f01fa57bf0d65bb`。

## 当前 h 的计算与固定数值

对轴 a ∈ {x,y,z}，定义平均间距 `mean_delta_a = (a_max − a_min)/(N_a − 1)`，再令 `h_unrounded = min(mean_delta_x, mean_delta_y, mean_delta_z)`。正式查询使用用户确认的下表固定常数，不能默默改用未舍入值。

| 流场 | 平均 Δx | 平均 Δy | 平均 Δz | 最小值所在方向 | 固定 h |
|---|---:|---:|---:|---|---:|
| Channel | 0.0122718465094473 | 0.00613592307605044 | 0.00390949206171083 | z | **0.003909492** |
| TBL | 0.292218128551136 | 0.117239990234375 | 0.118764224080077 | y | **0.117239990** |

两个原始场的 z 网格非均匀。这一定义是代表性平均尺度，不声称所有网格单元具有相同边长。旧实际最小相邻间距 Channel `0.0000165104866`、TBL `0.007235100027` 不用于本版本查询；2.3 的 Channel `0.00613582134` 也已被取代。

## 邻居分布可视化

版本 `Verify_Task4C_BallQueryCounts_2.4`；沿用冻结 v2 r3 的 Channel 189416 / TBL 198413 个播种点，不重建曲线。数据审计 SHA-256：`e5aaa4fb8a8e01882c461da5916b3bb96883e2787b231b018b512d7e30d3a2a8`。

球半径为 `r*h`，统计同流场全部其他播种点，不计中心自身，包含边界。每个邻居在指定曲线长度下提供一条线，不把三种长度计成三个邻居。界面按训练/验证/测试或标签筛选时，只筛球心，邻居池不变。统计不扩球、不执行最远点采样。

重算24档半径：0.25、0.5、0.75、1、1.25、1.5、2、2.5、3、4、5、6、8、10、12、16、24、32、48、64、128、256、512、1024h。网页同时显示两个流场的 h、范围、网格分辨率、实际查询半径、直方图、最少/平均/中位数/最多邻居，以及零邻居和不足6/16个的比例。

| 版本 | h 定义 | 配置 / 主代码 | 状态 |
|---|---|---|---|
| 2.2 | 所有实际相邻坐标差的最小值 | `Analyze_Task4C_BallQuery_2_2.py` | 历史统计；旧1h训练已取消 |
| 2.3 | 指定 Channel Δy、TBL Δz 的最小实际间距 | `Analyze_Task4C_BallQuery_2_3.py` | 历史统计 |
| **2.4** | **三方向平均间距取最小值，并固定为用户确认的常数** | `config/Verify_Task4C_BallQueryCounts_2.4.json`、`experiments/Analyze_Task4C_BallQuery_2_4.py` | 当前统计；无训练任务 |

生成网页：`experiments/Build_Task4C_BallQueryViewer_2_4.py`；模板：`experiments/templates/task4c_ball_query_counts_2_4.html`。输出目录 `outputs/Verify_Task4C_BallQueryCounts_2.4/` 保存原始网格核验、逐样本计数、固定 h 文件及网页；`radius_statistics.json` 的 `grids` 保留完整坐标范围和各轴平均间距，`flows.*.verification` 保存独立欧氏距离复算结果；`viewer/manifest.json` 保存统计与网页哈希。

核验包括原始场及冻结数据文件哈希、网格尺寸与范围、h 与平均间距公式一致、全部样本计数随半径单调不减；对固定随机球心及各半径极值球心直接扫描全流场所有样本点，逐一复核24档计数。仅在全部核验通过后生成最终统计和网页。

本次已完成全部387829个样本的24档统计，Channel62 / TBL64个球心的独立距离复算全部通过；统计 JSON SHA-256 为 `3e65d971418a538fcbabc9df4de627fc7ce6069842786550b069c6e69d3e66eb`。网页1h→4h切换与统计一致：4h下 Channel最少0、平均26.825907、最多70，TBL最少0、平均55.277890、最多183。浏览器未报告脚本错误。未提交训练。

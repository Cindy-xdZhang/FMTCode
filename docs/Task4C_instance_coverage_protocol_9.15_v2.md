# task4-c-v9.15_v2：按用户要求恢复4.14规则

2026-09-15最新用户指令优先：撤回完整13实例留出、局部种子标签、每实例500正500近邻负和固定150轮策略；保持版本名称v2，只扩展积分长度和加密底部Hairpin采样。本文取代此前同名协议中的错误设置；错误运行的源码、数据和日志保留于commit `9769c68d`及原Ibex目录，明确标为废弃。其FMT分数不得作为修正版v2结果。

## 恢复项

直接复用冻结`experiments/Task4C_PhysicalLength_4_14.py`的候选头区、整头区标签、空间块、距离检查与基础采样；原文件不修改。每次加载配置逐项检查flows、labels、bundles、local_split、旧sampling及training参数，拒绝静默变更。

- lambda2阈值−13.395/−0.0272，八顶点约束、正展向脉动涡量和至少10个头区单元保持。
- 全头区GT单实例占比≥50%为正，零覆盖为负，混合排除；实例0有效。旧规则本身排除的GT实例不改标补入。
- 同头区空间块3/1/1，评价中心到训练中心至少1h、到同头区训练中心至多4h，test到validation至少0.5h。**不专门留出完整实例（0个）**；正式核验要求所有评价正类实例和头区在训练中出现。
- 原负类候选头区与负类播种函数不变，不再按GT附近实例分配负样本，不强制正负平衡，不使用新增每实例配额。
- 邻居距离恢复0.25/0.5/1h。
- h0结构、初始lr0.001、dropout0.15、weight decay0.0001、batch128保持；最多500epoch，验证损失平台15epoch降半、最低lr1e−6，50epoch无验证F1/AP改善早停。恢复验证最佳权重后才测试一次，固定0.5阈值。种子恢复96611–96613。

## 仅保留的采样变化

总量按用户此前确认保持132,000束，并沿旧27,000/3,000/10,000比例扩大为89,100/9,900/33,000；每流场44,550/4,950/16,500。比例与角色恢复，数量不再按实例类别强配额。

Channel单向长度0.08–0.12、11档；TBL3–5.5、12档；ds分别0.001–0.005与0.01–0.05。双向全线0.16–0.24/6–11。原RK45清洗后再核验用户给定弧长范围及32点重采样全线长度，保证至少10条有效线。具体整数步数和ds见配置。

底部加密只改变正类头区内的播种位置：偶数中心编号仍按旧完整分区单元池选择；奇数编号使用该池中z不高于中位数的单元。负类对相同输入逐值调用旧函数。它不重定义head或改变标签，不改变正负头区轮询次数。中心编号跨尺度保持，同一中心不会跨集合。

## 网络

按已指定的p35/n0_k06：最大半径归一化、6频率（含零频）、逐特征均值＋数值最大值、仅训练有效线逐特征标准化。复用冻结编码器和h0，141维、76,738参数。Conv32³/36³/48³同72,192参数（94.08%）；四通道体素与原稠密float16逐值一致，稀疏缓存只改变存储。确定性池化数学窗口与梯度保持。无checkpoint文件。

## 代码、验证与部署

仍使用原v2的config、两份Python文件及Slurm脚本；`execution_revision=restored_4.14`只区别本次纠正和废弃运行，不是新实验代号。输出子目录`outputs/mainExp_Task4C_InstanceCoverage_9.15_v2/restored_4.14`。Ibex独立目录`/ibex/user/zhanx0o/FMT_Task4C_v2_Restored_20260915`，原错误数据不覆盖或复用。

依赖链：V100工程检查→两流场真实小预检→实测缓存容量→两流场正式构建→全量标签/空间/实例/长度核验→两流场编码→四方法×三种子训练→预测复算汇总。前一步失败阻止后续；失败、取消和运行身份均进入Ibex登记。

本地已检查100次负样本与旧采样逐值一致，正类50个偶数中心保持原样、50个奇数中心落在下半部分；CPU四模型、标准化、体素和确定性池化检查通过。真实流场预检与正式作业状态见运行登记。

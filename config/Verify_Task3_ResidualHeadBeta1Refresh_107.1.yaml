experiment: Verify_Task3_ResidualHeadBeta1Refresh_107.1
output_root: outputs/Verify_Task3_ResidualHeadBeta1Refresh_107.1
task: Task3

# Frozen while 77.1 remains incomplete and before 105.1 can produce any
# performance metric. The independently audited 106.1 portfolio supplies one
# complete recipe per physical family. The exact source is the control; the
# remaining candidates change only downstream residual-head Adam beta1.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_ResidualHeadBeta2RefreshPortfolio_106.1/portfolio_selection.json
    expected_experiment: Verify_Task3_ResidualHeadBeta2RefreshPortfolio_106.1

allowed_source_overrides: [training.residual_head_optimizer_beta1]

# c00 preserves the complete 106.1 source recipe. Each remaining cell replaces
# only beta1 for trainable parameters outside fmt_encoder. The head inherits
# source beta2; the auxiliary projection keeps its source betas and other
# optimizer settings; the frozen Raw backbone remains excluded.
optimization_candidates:
  - {id: c00_control_source, sources: [portfolio]}
  - {id: c01_headbeta1_000, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.0}}
  - {id: c02_headbeta1_025, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.25}}
  - {id: c03_headbeta1_050, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.5}}
  - {id: c04_headbeta1_070, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.7}}
  - {id: c05_headbeta1_080, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.8}}
  - {id: c06_headbeta1_085, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.85}}
  - {id: c07_headbeta1_090, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.9}}
  - {id: c08_headbeta1_095, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.95}}
  - {id: c09_headbeta1_098, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.98}}
  - {id: c10_headbeta1_099, sources: [portfolio], training: {residual_head_optimizer_beta1: 0.99}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: c00_control_source
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.246
  target_absolute_fmt_f1: 0.906
  confirmation_opened: false

experiment: Verify_Task3_AuxiliaryPostNorm_75.1
output_root: outputs/Verify_Task3_AuxiliaryPostNorm_75.1
task: Task3

# Development-only single-factor search.  The independently audited 74.1
# portfolio supplies one frozen recipe per physical family.  Candidates change
# only a fixed, parameter-free normalization immediately after projection.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_AuxiliaryFeatureScalePortfolio_74.1/portfolio_selection.json
    expected_experiment: Verify_Task3_AuxiliaryFeatureScalePortfolio_74.1

# n00 exactly reproduces the source graph. Other candidates act after the
# projection activation and before Dropout, Gaussian noise, and fixed scaling.
optimization_candidates:
  - {id: n00_control_none, sources: [portfolio]}
  - {id: n01_center, sources: [portfolio], model: {auxiliary_post_normalization: center}}
  - {id: n02_rms, sources: [portfolio], model: {auxiliary_post_normalization: rms}}
  - {id: n03_layer, sources: [portfolio], model: {auxiliary_post_normalization: layer}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: n00_control_none
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.216
  target_absolute_fmt_f1: 0.893
  confirmation_opened: false

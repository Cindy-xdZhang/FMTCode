# Task4-c FMT 学习率与正则化调参 1.1

2026-09-20用户明确要求调查“第2–4轮之后无法训练”，并大力调参。版本`Ablation_Task4C_FMTOptimization_1.1`。只用seed96611。已完成的`mainExp_Task4C_V2NewLabelTraining_1.1`（df311553）保留作冻结对照，不修改其预算或结果。

## 已核实的现象

第2–4轮是最佳测试轮，四FMT实际完成52–54轮。880,530训练行、batch128，每轮6,880次更新，第2–4轮已有13,760–27,520次更新。c156 ball6训练损失0.365113→0.077265，测试损失0.905879→2.206025；学习率第1–17轮0.001，第18/34/50轮才降为0.0005/0.00025/0.000125。训练集能拟合，测试表现恶化，符合过拟合现象；仅凭现有日志不能断定初始学习率是唯一原因。测试召回率偏低，误差以假负为主。

c156 ball6所选第2轮按短/中/长曲线测试F1为0.579403/0.517536/0.459210。长曲线指标更差是观察结果，不据此证明标签规则错误。标签、三长度展开方式、数据及折保持不变。原始诊断见`outputs/Ablation_Task4C_FMTOptimization_1.1/diagnostic_before_search.json`。

## 预先固定候选和预算

p35/h0、c156各ball6/16，四种设置。每种设置比较下列方案；固定batch128、AdamW、梯度裁剪5、阈值0.5及网络参数量。

| 方案 | 初始学习率 | weight decay | Dropout | 降学习率patience |
|---|---:|---:|---|---:|
| 冻结对照，复用既有前12轮 | 0.001 | 0.0001 | 原设置 | 15 |
| lr3e4 | 0.0003 | 0.0001 | 原设置 | 15 |
| lr1e4 | 0.0001 | 0.0001 | 原设置 | 15 |
| lr3e5 | 0.00003 | 0.0001 | 原设置 | 15 |
| lr1e3_decay2 | 0.001 | 0.0001 | 原设置 | 2 |
| lr3e4_wd1e2 | 0.0003 | 0.01 | 原设置 | 15 |
| lr3e4_drop35 | 0.0003 | 0.0001 | 所有nn.Dropout为0.35 | 15 |

共24次新筛选，**每组12个完整训练轮，无筛选早停**；原对照使用已有前12轮，另用新驱动独立重放第一轮验证实现等价。降学习率仍为测试损失驱动的ReduceLROnPlateau，factor0.5、threshold1e-4、min_lr1e-6。正则化两个方案分别与lr3e4对照，提前降速方案与冻结0.001对照，便于区分改变了什么。

每种设置按前12轮最大完整测试F1排序，平均精度打破平局，再按方案ID稳定排序。每种设置最多一个入选方案：若冻结对照获胜，复用其已完成结果；否则只将该方案以**同一个seed96611**从头训练到最多100轮、20轮无提升早停。至多4次延长训练，不加种子、不做未声明搜索轮次。该100/20预算只适用于新调参候选，不追改旧500/50对照。结果明确标记测试集参与超参数选择和早停，不能称独立留出测试评估。

## 数据、实现与诊断

继续使用冻结`v2_newlabel_rule`，audit SHA256 `9a983a79107798af4fd9eb86dce2c2e9b5cbc143c12e646dd5bf438ea5f81a95`；四折全训练880,530行，一折测试290,580行，无验证集。保留3h扩球/完整球内FPS、同长度邻居、最短曲线标签、三长度等权展开。标签不进入测试梯度，拟合特征统计仅用训练集。

复用旧四组已生成的141维特征和训练集标准化统计，逐文件固定哈希、检查数值有限；p35符号对数变换已在缓存内，不重复变换。每种设置随机数初始化、完整训练顺序和第1轮参数更新先与旧驱动核对，训练/测试损失误差<1e-7、测试F1完全一致；真实训练样本独立重编码与复用缓存对照通过才允许筛选。Dropout覆盖仅在新模型实例上执行，冻结网络源码不改、参数量不变。

每轮报告完整测试合并/分流场/分长度指标、实际学习率、优化步数、梯度范数均值/最大值与被裁剪步骤比例。同时评价固定8,192条训练行的训练表现，标记为train_probe；这些行仍参与每轮训练，不是验证集，也不用于筛选。最佳预测即时保存，结束后完整训练集和测试集预测逐行复核。未更改损失类别权重、阈值、数据划分或长度融合。

私有部署、最多6张V100并行。源/缓存核验→四个GPU原始首轮重放→24组筛选（并发6）→固定规则选择→至多4组延长训练→预测复算汇总。每组可独立查看进度，无需等全组结束。实际提交和状态另写执行记录，不把已提交当结果改善。

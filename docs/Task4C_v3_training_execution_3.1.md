# Task4-c v3 单种子训练执行记录

用户2026-09-19要求v3构建完成后部署各方案测试F1。冻结数据7,762,378点，审计SHA-256 `b584fc8e8f42dcc305754e550804eb3f8fb75c254fd76cf7272a9c6623312584`。协议 [Task4C_v3_training_protocol_3.1.md](Task4C_v3_training_protocol_3.1.md)。

邻居计算科学commit `86b66de5a3b9da6548503d17d77a1814d225ba7f`，独立目录 `/ibex/user/zhanx0o/FMT_Task4C_V3Neighbors_3p1_20260919`。训练科学commit `55ab3255778c5e4e0f240f53ff06034326348893`，独立目录 `/ibex/user/zhanx0o/FMT_Task4C_V3Training_3p1_20260919`。后者只链接冻结数据和共享邻居，不重复展开完整曲线簇。

## 提交及启动

2026-09-19 18:17:16 UTC：Channel邻居 `52109791_0` 已成功（10分43秒），TBL邻居 `52109791_1` RUNNING；十组正式训练已提交，按成功依赖等待。尚无正式测试F1。下表任务只提交一遍。

| 阶段 / 方法 | 邻居数 | Slurm作业 |
|---|---:|---|
| 共享邻居表 | 6和16 | 52109791_0、52109791_1 |
| 源数据与邻居哈希核验 | — | 52109933 |
| 十组GPU预检 | — | 52109934_0–9，并发4 |
| p35/h0 FMT | 6 | 52109935_0 |
| p35/h0 FMT | 16 | 52109936_1 |
| c156 FMT | 6 | 52109937_2 |
| c156 FMT | 16 | 52109938_3 |
| Conv3D16³ | 6 | 52109939_4 |
| BiLSTM | 6 | 52109940_5 |
| PointNet | 6 | 52109941_6 |
| Conv3D16³ | 16 | 52109942_7 |
| BiLSTM | 16 | 52109943_8 |
| PointNet | 16 | 52109944_9 |
| 预测独立复算 | — | 52109945 |

均只用seed96611，3h起步、不足扩球、超额完整球内FPS，保留原训练500轮上限/50轮早停。每组在自己的GPU预检成功后放行；未追加种子、候选或修改v2作业。预检仅检查输入、损失下降和显存，不能当正式测试成绩。

## 已核验

- 本地及远端八项测试全部通过；几何输入逐值匹配旧导出，主机缓存索引、p35标准化及邻居算法等价。
- 训练部署包SHA-256 `7f668af6dcdee23972ba96f2c43d38c9d34736cb183102a5ae81c2d2711d7828`；远端75份Git固定源码/配置哈希一致。
- 邻居包SHA-256 `3b18491d00052c4e2e22b37af27a053b742eb12d3771378d030eac293861fb20`。首次SSH脚本尾行CR导致sbatch找不到文件，未产生作业；核对后仅提交上述52109791数组。
- 其他会话对本地旧驱动的未提交改动未打包。训练运行在固定Git独立目录，使用其已提交旧驱动与原训练循环。

证据目录 `outputs/Ablation_Task4C_BallQuery_3.1/remote_evidence/`，含`submission.json`、`submissions.jsonl`、带时间戳的`progress_latest.json`。只有正式预测与独立复算均完成后才报告最终测试F1。

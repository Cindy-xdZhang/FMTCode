# FMT v8.1：五组几何表示比较

> **2026-09-17撤销状态：** 本文涉及的Raw卷积＋FMT/残差或二维FMT卷积网络，已按用户要求从可用方案及有效FMT证据中删除；旧数值仅为撤销历史，不得用于当前主表、附录或“最佳FMT”结论。固定傅里叶特征本身及明确无卷积的Task1/Task4-c分支不受此撤销影响。以 [FMT禁止空间卷积协议1.1](FMT_no_spatial_convolution_protocol_1.1.md) 为准。


用户在2026-09-14指定V8为拟用于论文的主要新模型，要求与原FMT、fmt_v5比较Task1、Task3、Task5和Task4-c 4.14；随后明确要求233/398/100/161/326维全部并列。Task2不在此次范围。以下配方先于新测试结果固定，不预设V8胜出。

| 标识 | 实际特征组合 | 维数 |
|---|---|---:|
| fmt_161 | 中心步进频域特征23＋邻居相对变化频域特征138 | 161 |
| fmt_direction_233 | 原161＋中心线坐标/单位切向方向频谱72 | 233 |
| fmt_v5_326 | 原161＋15个中心夹角的频谱165 | 326 |
| fmt_v5_direction_398 | 原161＋方向72＋角度165 | 398 |
| fmt_v8_100 | 中心步进频域特征23＋方向频谱72＋邻居频域138项的带符号top4＋带符号均值1 | 100 |

上表全部分支均经过DFT。中心位移序列先DFT，再由复系数计算模长、内积和三重积描述量，共23维；六个邻居相对位移变化序列各经相同过程，共138维。角度165维直接保留所选DFT复系数的实部和虚部。V8池化发生在DFT及其后描述量计算之后，不是对原始几何坐标池化。“中心23/邻居138”仅是这些频域特征的简写。

代码`FMT_Utils/fmt_v8.py`，版本8.1。V8实际数组为中心`[0:23]`、方向`[23:95]`、池化`[95:100]`。对未做逐维标准化的原138维邻居特征按绝对值降序选前4项，保留符号；绝对值并列时按原列顺序。均值为全部138个原值的算术平均，包含结构零位，不取绝对值。用户已明确确认符号规则。池化在signed-log/训练集标准化之前，之后按各任务原预处理执行。没有学习到的排序、可训练编码器、kin、curl或IVD估计输入。三维坐标的有方向频谱使整体不具有一般时变观察者客观性，不据此宣称客观。

方向72维严格复用4.14公式：32个中心线坐标与单位前向切向（末端复制倒数一段）拼成6通道，沿样本轴正交归一化实FFT，保留0..5频率的实虚部，共72维。频率轴为存储样本序号，不称物理Hz。Task1/3/5为物质轨线，Task4-c为静态快照中的涡线，物理含义分别报告。

## Task4-c

版本`mainExp_Task4C_FMTv8_4.21`；原4.14全部数据、训练配置及V100固定。五个组合均重新训练96611/96612/96613三种子，共15次训练。233和398维保留旧完整输入；161和326维是无72维方向分支的对照。原161、方向72、角度165来自已核验冻结缓存，只按列拼接/池化，不重新采样。每线另附有效线标记，模型输入宽度分别162/234/327/399/101。

分类器复用逐线128维映射、有效线mean/max聚合及原分类头；233维直接使用原初始化，其他宽度使用现有适配器。参数分别79,298/88,514/100,418/109,634/71,490；不能将五组称为严格相同参数量。原27000训练/3000验证/10000测试，AdamW 0.001、dropout0.15、weight decay0.0001、batch128、最多500轮、验证F1/AP早停50轮不变。验证选模型后固定0.5阈值测试一次，复用已使用的同头区局部空间benchmark，不宣称独立涡实例泛化。

每组报告F1、参数量、训练至最后验证耗时及首轮以后每轮耗时；计时不包括旧几何/编码缓存构建和最终测试。读取缓存后池化的时间不能代表完整V8在线编码代价。

小Conv3D参照采用用户独立修订值0.7499±0.0126。用户说明旧数据核验有问题，已用另一套代码复核，要求不再复查。参数83,918及18.432分钟为原4.18运行元数据；新逐种子和分流场值尚未提供，不能用旧分项替代修订后结果。原0.789915±0.012577已在论文表和实验记录标明被该用户修订取代。

## Task1/3/5

版本`mainExp_Task135_FMTv8_1.1`。沿用既有10个3D条目、p95标签、数据划分、三种子和冻结训练器，Cylinder初始时刻至少7.5。Task5的18/6/9个训练/验证/测试尺度组合及原积分设置不变。

原161维采用冻结缓存，保留原scale1/weight1的历史数值；两种角度组合只追加冻结fmt_v5的165维角度。方向分支在每个七线primitive内部以全部224点质心和最大半径归一化，再取中心线生成72维谱；该新方向分支不改原161维。五组公共分支逐值一致。V8核心池化算法统一，输入缩放遵守各任务冻结原FMT配方；Task4-c的原邻居scale100/weight0.5与Task1/3/5缓存scale1/weight1分开说明。

Task1使用原训练集标准化/PCA8及KMeans，验证集仅确定涡簇编号，三种子7380/7381/7382；同时保留Raw聚类对照。报告编码、聚类耗时，编码器无可训练参数。

Task3/5复用原Raw骨干＋几何残差分类流程，并保留Raw、Raw-wide和Raw-PCA对照，Task5保留固定Task3 Raw迁移。五组辅助表示均补零到433维，网络架构、总参数和可训练参数保持相同；不能以本实验补零输入宣称V8减少了分类网络参数。训练种子40/41/42、batch512、学习率0.001、weight decay0.0001、最多100epoch、patience20及原融合选择不变。所有方法训练和验证选择完成后才读取同一已用测试benchmark。报告训练原Raw骨干的公共开销和每个残差分支的新增开销，避免遗漏骨干训练成本。

Task1/3/5所有161/326维纯几何结果均为新配对实验，不能与先前附有kin4/gram2/kin6的“原FMT”简写结果混合。五组不使用这些附加输入。

## 完成条件与保留

源码commit、完整配置、数据哈希、每个Ibex进程提交/起止/节点/GPU及失败记录均登记。工程检查覆盖带符号top4、稳定并列、均值、NumPy独立复核、真实训练数据、公共特征一致、相同分类容量及原训练器调用。最终保存逐样本预测、逐次指标、分流场/尺度结果和逐种子10数据条目平均值；总体种子标准差不混入不同流场波动。逐样本指标独立复算后，删除Task3→Task5依赖链的临时模型，不下载权重。方法结论只写`docs/experiment_log.md`。

## 全部完成

科学commit `0ae17ea218394b6c7ed1cb8f897e5893d0c71dfa`，配置`config/mainExp_Task135_FMTv8_1.1.json`，SHA256 `c3e7ea9765bb7bafe9dad91645feb76480d26c0e0aa1b02c360a4c462c97b950`；summary SHA256 `b1774f5ed4be2db625e234438274bc26e7185bf97caf4a5db9f9cb7270a68107`；完成审计代码commitaa0437f1。

预检51892661、小样本51892662、Task1 51892691[0–29]、Task3→5 51892692[0–29]、汇总51892705全部完成，共63个Slurm进程与126条起止事件。90个科学分片、690行逐次结果、Task5逐尺度指标、原始源标签与预测逐样本身份独立核对；每条Task3→5链依赖结束即删除临时checkpoint，最终无权重文件。Task4-c结束后将Task35并发从6改为12，复用已释放的GPU槽位，合计并发上限仍12，科学配置未变，变更已登记。证据位于`outputs/mainExp_Task135_FMTv8_1.1`及个人Ibex `FMT_Task135_FMTv8_20260914`；证据包SHA256 `0e82adc2160fa9cc4e3f17fa0db8b055a9984fe718b335231ef909fa4d61e968`。

Task4-c另有17项任务、34条事件及15次新训练全部通过，V8测试F1为0.728480±0.004107。完整方法结论见experiment_log中两个版本的结果条目。

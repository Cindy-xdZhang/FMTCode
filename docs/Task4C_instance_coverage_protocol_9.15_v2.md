# Task4-c-v9.15_v2：增加长度种类、保留head必选束、加入分类分析

用户名称`task4-c-v9.15_v2`；实验名`mainExp_Task4C_InstanceCoverage_9.15_v2`。v9.15及4.14数据、源代码和历史结果保留。2026-09-15用户已确认单向长度并授权正式构建与四模型比较；旧开发/预检记录保留。当前部署状态见Ibex登记。

## 已确认的数据量与单向长度定义

2026-09-15用户明确选择保持132,000束，只增加长度种类：每个实例500束Hairpin＋500束Non-hairpin。Channel74个、TBL58个实例，119个训练实例、13个测试实例，119,000训练＋13,000测试。采用原v9.15相交扩张采样盒分组及固定实例划分，测试实例不向训练提供必选head束。两个集合，不增加第三份。

2026-09-15用户明确确认：Channel[0.08,0.12]、TBL[3,5.5]均指单向积分弧长；双向整条曲线范围分别为[0.16,0.24]和[6,11]。ds分别限制[0.001,0.005]和[0.01,0.05]。正式配置为`length_definition=per_direction`，并写入固定整数步数与实际ds表；这取代此前的待确认状态。

`configure_lengths`已将唯一选定的单向积分表写入正式配置。Channel11档、TBL12档：范围内11个等距目标（边缘向内移动区间宽度的1%），另保留Channel0.1、TBL5的名义值；不重复已有名义值。以预定ds顺序和整数步数计算每档实际ds，严格保持用户ds上下界。结合四档邻居距离，Channel44种、TBL48种尺度组合。相同中心可使用多个长度，但不复制相同几何补配额。

每条原始半线弧长、双向总弧长、32点重采样折线弧长及实际步数均记录。除冻结RK45原有完成度检查外，新清洗要求原始积分与实际模型输入曲线都在指定长度范围（只允许相对于总长度上界1e−6的数值容差）；不足10条有效线整束剔除。单向定义时整条曲线检查[2Lmin,2Lmax]，双向定义时检查[Lmin,Lmax]。不以改变几何缩放冒充积分长度变化。

## Head必选束与标签

v9.15已经实现每实例3束必选head；此前132实例小预检共396束必选head通过。head在GT内部按速度与涡量的锐角严格大于45°判定，即`abs(u·omega)/(||u||||omega||) < sqrt(0.5)`，零向量排除，反平行与平行合并。实际播种点仍须满足原lambda2阈值和正展向脉动涡量；Channel使用存储涡量，TBL采用原生网格速度二阶差分curl。

v2继续每实例3个不同中心必选head束，中心线被清洗删除则该必选束不合格。用户已确认的有效播种点局部标签保持：至少50%属于唯一同一GT实例为正类；全部在GT外为负类；混合及并列归属剔除。跨集合GT相交检查、GT实例0有效、原始涡量双向积分、整束质心/Rmax归一化保持。

## 网络与GPU预检修复

当前用户指定p35的`n0_k06`配方：逐特征均值＋数值最大值池化，整束最大半径归一化，训练集有效线逐特征均值/总体标准差，保留含零频在内的6个频率。直接复用`FMT_Utils/FMT_P35_NormFrequency_3_1.py`，邻居倍率/权重均为1，不再沿用开发版的signed log1p与[-8,8]截断。保留h0分类器，141维、76,738参数；Conv32³/36³/48³均72,192参数，为p35的94.0759%。冻结三种子91511–91513、150轮、batch128、AdamW、学习率0.001→0.00001、dropout0.15、weight decay0.0001、阈值0.5，最终轮次才评估测试集，不用测试挑选设置。

核查发现原v9.15 GPU预检51907304于2026-09-15 12:50 UTC失败，原因是`adaptive_avg_pool3d_backward_cuda`不支持严格确定性；47个后续依赖进程均在开始前取消，故原v9.15未完成正式采样或训练。此前记录“已提交”不代表已构建。失败源码`433c71b4`与日志保留。

v2第一次独立GPU预检51908822（源码`4bc96e6b`）通过平均池化的独立梯度对照后，又遇到`max_pool3d_with_indices_backward_cuda`不支持严格确定性。现将末端平均池化改为同一2×2×2自适应窗口的切片均值，将两次非重叠最大池化改为同一窗口的重排与最大值归约，包含并列最大值选择首个位置的原规则；参数量及数学窗口不变，不关闭严格确定性。CPU前向/反向及并列最大值检查通过，三种体素稀疏还原逐值相同。

第二次独立V100工程预检51908979，源码`af3923327c22a9cd7f1ddb92621aa1939cbd3996`，只测试p35和三种分辨率的前向、反向、优化步骤，不读新物理样本。配置SHA256`03ec90eb44c2e019cfeff5c4e7fed151e6a43d98e053562487213f8dec306797`。最终状态见Ibex登记。正式数据未提交，不应报告v2 F1。

## 交互分析

共用查看器增加“GT Head附近线簇＋Non-hairpin”模式：保留真实正类且中心在GT head内的全部候选束，以及真实Non-hairpin束。此模式数量控件按真实Head/Non-hairpin分组，颜色仍由模型预测决定，因此不会把漏检head从候选中排掉；切入模式默认全部head，Non-hairpin数量可单独调整。普通模式仍按预测类别分别控制数量。

分析勾选按局部束标签和0.5阈值区分：TP正确正类、TN正确负类、FP误报、FN漏检；线和中心点同色。显示筛选不改变完整集合F1。旧包缺少head字段时，在实际中心用原场速度/涡量与原GT重新计算；v2包直接保留采样元数据中的head标记和夹角。

v2训练完成后导出全部132,000束，取消旧每实例20束展示子集；几何分片按需加载。新数据页初始显示1500束head和100束Non-hairpin，以控制首次渲染量，用户可选择全部。当前已生成的`outputs/Other_Task4C_BundleVisualization_1.3/viewer/index.html`使用已有4.14的37,000束及已完成模型预测，并非v2结果。原1.1和1.2链接均转至1.3，历史HTML和几何保留。

真实数据检查：新旧37,000束坐标、标签、概率及292个几何文件完全一致；head判定与独立SciPy三线性插值及GT包含判断相同。Head模式候选数量：Channel训练1883/测试713，TBL训练1437/测试517。Node检查四类真值表、0.5边界、全部漏检head仍被纳入通过。浏览器交互未验证：此前本机origin访问被自动审批拒绝，没有改用其他路径绕过。

## 代码与证据

- `FMT_Utils/Task4C_InstanceCoverage_9_15_v2.py`：新长度表、额外长度清洗、灵活尺度采样；复用冻结v9.15源场、GT和标签函数。
- `experiments/Task4C_InstanceCoverage_9_15_v2.py`：独立v2入口、池化修复、全量导出。
- `config/mainExp_Task4C_InstanceCoverage_9.15_v2.json`：132,000束、已确认单向长度表与固定训练设置。
- `ibex_bash/task4c_instance_9p15_v2.sh`：v2开始/结束记录。
- 模型CPU检查、两种长度定义46项解析场检查（1242条线）见`outputs/mainExp_Task4C_InstanceCoverage_9.15_v2`；解析检查只验证实现，不替代用户选择或真实数据检查。
- 查看器证据位于`outputs/Other_Task4C_BundleVisualization_1.3/geometry_and_head_audit.json`和`head_analysis_checks.json`。

正式依赖顺序：GPU预检→全部132实例真实场小预检→测量稀疏缓存空间→正式数据构建→独立重读全部132,000束检查长度、标签、head、重复几何与实例划分→统一编码→p35与三种Conv3D各三种子训练→汇总和全量查看器数据包。任一步失败阻止后续阶段；不修改标签或放宽长度补配额。新旧版本不可混写。

GPU完成补记：51908979于2026-09-15 13:28:38–13:28:51 UTC在gpu609-09运行完成0:0；p35及三分辨率Conv3D的前向/反向/优化均通过，平均池化与CPU参考最大误差2.22e−16、最大池化含并列最大值的前向/梯度逐值相同。实际结果与运行事件为`preflight_cuda.json`、`preflight2_runtime.jsonl`。这仅证明网络工程检查通过，不表示新数据构建或150轮正式训练完成。

存储检查更新：Ibex启动前可用约69 GiB，旧100 GiB固定门槛替换为全部132实例792束pilot的实际三分辨率稀疏缓存测量。按132,000束外推并乘2倍余量，另计几何、元数据、特征、查看器副本及10 GiB保留空间；不足即阻止正式构建。编码期间每实例开始前再次检查10 GiB保留空间。不删除历史文件。

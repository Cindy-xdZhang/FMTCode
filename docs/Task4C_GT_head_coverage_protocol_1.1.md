# Task4-c：逐GT头部补样与c156分类可视化1.1

2026-09-17用户要求每一个人工标注Hairpin头部都能看到测试线束分类结果，并向训练集加入邻近线束。用户随后明确：使用固定最佳FMT c156；新增束按人工GT头部归属标为正类，旧标签不变。

## 已核对的缺口

`Verify_Task4C_GTHeadCoverage_1.1`直接核对当前第三页的全部37,000束，并以原始GT体单元查询中心归属。测试头部覆盖：Channel 55/74，TBL 46/58；训练头部覆盖62/74、53/58。没有被负标签隐藏的GT头部中心。原页面包含全部原始行，缺口不是导出抽样上限。

旧采样链要求λ₂候选、正oyf、至少10个连通单元、整片候选区域的同一GT占比至少一半，再经过空间划分与积分清洗。重新读取原流场和GT后发现，Channel10个、TBL3个实例没有属于自己的合格正类候选区域；132个实例却都有满足点处λ₂、oyf与速度–涡量锐角>45°的GT内头部点。仅扩大旧采样次数不能保证全覆盖。证据在 `outputs/Verify_Task4C_GTHeadCoverage_1.1/{coverage,catalog}.json`，逐实例原因与行号保留。

## 新数据方案

新版本 `mainExp_Task4C_GTHeadCoverage_1.1`，在冻结BottomDensity1.2的193,000训练、3,000验证、10,000测试束后追加。全部原数组前缀与标签保持，验证文件原样保留。

预定每个GT实例10束新增头部测试、每束3束邻近训练：132个实例合计1,320测试、3,960训练，目标总量196,960/3,000/11,320。不根据模型预测或错误率决定实例配额。先进行全部132实例的坐标可行性及小积分预检，全部通过后才生成正式数据。若某个实例无法满足约束，记录具体失败条件，不静默跳过或放宽。

新中心须在指定GT内，点处λ₂低于原流场阈值、oyf为正，速度–涡量锐角严格>45°。新增标签由用户指定的人工GT头部归属给出，不再要求整片连通候选区域多数归属；这一变化仅适用于追加样本。邻居仍取三维3×3×3偏移，间距来自原0.25h/0.5h/1h；邻居以点处λ₂与oyf筛选，不按GT裁切线。沿完整原涡量场做原RK45双向积分与长度清洗，每束至少10条有效线，保存每线32点，再由固定c156重采样48点。

坐标预检使用原距离范围：新增测试距全部训练至少1h，所配同实例新训练在1–4h之间；新增训练也避开全部原验证/测试及其他新增测试至少各评价中心的1h；测试距验证至少0.5h。邻近样本属于共享GT实例的局部评估，不称未见实例泛化。原完整头区的五块分配保持在旧样本中，追加采样以新GT配对规则单独记录。

## 固定模型与检查

仅固定c156：p35、六邻居最远点采样、48均匀点、加宽残差网络、无增强、992,386参数。为交互查看生成一份模型预测，固定使用原最终种子列表中的第一个96721，不根据结果选种子。沿用500轮上限、50轮早停、原验证F1及平均精度选轮、学习率0.001、权重衰减0.0001、Dropout0.15、批128、阈值0.5。无新模型搜索，不把单种子可视化运行当三种子主表优势证据。

全部数据清单、源文件哈希、中心距离、GT归属、实际积分长度和逐实例配额核验后才训练。预测需覆盖每一行，并重新核对标签、概率及按实例计数。分别记录原测试子集与新增头部子集，不能把扩充后类别比例改变造成的整体F1变化归因于模型改善。新增子集均为正类，其核心报告是每GT头部的正确识别数和漏检数。

第三页将增加每个GT实例的头部样本数、实际显示数与正确/漏检数。按真实头部位置选择线束，不能只显示预测Hairpin而隐藏漏检。旧4.14页面、模型预测与科学数据保留，新结果以新版本呈现。完整实现、调度编号和完成状态在后续完成记录中填写。

## 全实例预检通过

坐标预检132实例全部有合法测试/训练位置。最初18次候选轮次的Channel积分预检未完成GT10、17、50；保留失败日志。增加几何候选尝试次数、不改变标签/长度/距离/清洗条件后，Channel在第49轮、TBL在第22轮各完成所有实例，实际预检上限72轮。正式数据有每实例10组，预先将上限设为512轮；这是数据生成失败重试预算，不是模型或超参数搜索。

最终预检528束：Channel测试74/训练222，TBL测试58/训练174。独立SciPy插值检查头部夹角、λ₂和oyf，原GT体单元归属、半线弧长、至少10条有效线、补零/归一化和全体空间距离均通过。新增测试到训练的最小距离除以当地h为Channel1.005795、TBL1.012519；没有放宽到1h以内。证据 `outputs/mainExp_Task4C_GTHeadCoverage_1.1/pilot/data_audit.json`；本地预检在工作代码下执行，最终科学commit以正式部署记录为准。

训练固定单种子96721。训练视图只导出新增3,960束头部训练样本，页面明确显示其为196,960完整训练集的子集；测试视图导出全部11,320束。全部完整训练/测试行均推理并保存预测，查看器展示数量不改变模型评价样本。

## 首次部署的写出修复

科学commit8fd6be94，Ibex52000820_0/1分别在第397/18轮凑齐Channel740/TBL580组（每组1测试＋3训练），随后均因旧数组末批不足512行时目标切片跨入追加区而失败。不是采样配额或物理清洗失败；旧源文件未写入，GPU训练未开始。失败目录保留，后续依赖任务取消并登记。修复为最后一批明确截到旧数组长度，增加对513、708、804行旧数组与新数组的独立拼接核验；重新部署独立r2目录，采样和模型配置不变。

## 正式数据和V100检查完成，分类训练进行中

r2科学commit为c3952a7b。52000893_0/1数据准备和52000894审计完成：196960训练/3000验证/11320测试，每个GT10新增测试＋30新增训练齐全；全部旧数组前缀与验证文件核验一致。新增测试到全部训练的最小距离除以当地h为Channel1.0006273875、TBL1.0005605686。数据证据为`data_audit.json`。

52000895在Tesla V100-SXM2-32GB完成32真实训练束的模型检查，参数992386，120步损失0.664362→0.000651，拟合F1=1。这只是运行正确性检查。正式52000896已进入V100训练，52000897等候成功后导出，尚无新正式F1。

为了让调度器利用较短空档，工程预检的Slurm时间上限从8h改为15min，正式训练从8h改为3h；同c156历史完整训练约34min，500轮上限、50轮早停和其他科学设置不变。独立本地收尾进程只等待这条已提交链，完成真实预测及配额复算后才生成工作台1.5；具体显示说明见`FMT_analysis_workbench_protocol_1.5.md`。

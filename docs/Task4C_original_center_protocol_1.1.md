# Ablation_Task4C_OriginalCenter_1.1

<!-- task4c-center-cleaning-clarification-20260917 -->
清理机制解释及措辞纠正：此前把957条缺失原中心都称作“实际长度不足”；准确结论应为“957条均触发双向物理弧长范围检查”。冻结`experiments/Task4C_PhysicalLength_4_14.py:141`将任一侧低于0.95L或高于1.002L均计入`insufficient_physical_length`，已有诊断没有保留逐条half_arcs，故该计数本身不能区分过短/过长。之前依据计数名字直接说全部过短，表述过强；此处更正，不改变样本筛选、代码或F1。

已核对当前数据与原GT构建commit c3952a7b的清理代码一致。播种阶段强制中心有效，后续`trace_batch`却只检查剩余有效线>=10，并按good掩码压缩曲线/播种点，不检查good[0]；metadata.center仍保存，因此产生“原中心被删但整个样本仍在”的数据。逐线依次检查有限数/点数、每侧弧长、连续重复点、任意坐标轴范围是否恰为0，再等弧长重采样32点。任意坐标恒定规则会删除例如完全位于xy平面的正常曲线，不能等同于一般几何退化，也不是旋转不变检查。本次957条在前面的长度门槛就已拒绝；后续axis_degenerate计数0并不能说明所有原始数据从未被轴规则过滤。

补积分两侧共1914次终止记录：880次码1、1034次码3；Ibex实际VTK9.5.0核实为OUT_OF_DOMAIN/UNEXPECTED_VALUE。它们分别表示出计算域/积分器报告异常值，现汇总未保留逐样本原因与长度配对，不能声称所有957条都是越界，也不能确定UNEXPECTED_VALUE的底层原因。当前只做只读诊断和解释，没有修改清理、重建样本或启动实验。证据center_reintegration_channel.json、center_reintegration_tbl.json；上述编码前清理发生于涡量场积分，不由分类器预测决定。
<!-- task4c-center-cleaning-clarification-20260917-end -->


<!-- task4c-original-center-results-20260917 -->
2026-09-17 `Ablation_Task4C_OriginalCenter_1.1`已完成：科学commitad0ffc85；同原中心有效子集196,042/2,993/11,288、seed96721和0.5阈值。p35/h0固定原中心/轮换对照测试F1=0.808841/0.885085，c156=0.843708/0.910726；分别下降7.6244/6.7018个百分点。固定中心参数76,738/992,386，训练23.623/45.548分钟；轮换对照25.262/33.065分钟。四组均最佳轮后50轮早停。8份预测、7成功进程/14运行事件、共同样本/中心索引/训练排列及科学源码独立核对通过。结果为单种子，无标准差；原数据及历史成绩不改。两种新方案严格原采样点中心，无卷积；旧轮换仅为本次配对对照。结果`docs/Task4C_original_center_results_1.1.md`，证据`outputs/Ablation_Task4C_OriginalCenter_1.1/independent_result_audit.json`。本项已完成，不追加实验，其他独立任务状态不变。
<!-- task4c-original-center-results-20260917-end -->


<!-- task4c-original-center-subset-authorized-20260917 -->
2026-09-17用户明确选择“使用原中心有效的共同子集”，解除下文历史待确认状态。固定使用196,042训练、2,993验证、11,288测试，唯一筛选条件为原采样点中心线存在；不看标签/预测筛选，不重新积分、不移动集合、不放宽清洗、不修改18个原数据文件。每流场每集合保存原行号和原中心索引及SHA256。Channel保留100,308/1,499/5,726，TBL保留95,734/1,494/5,562。

两种原中心模型加同子集旧轮换对照，共四次完整训练，统一seed96721，既有优化/模型选择规则不变，无搜索。四臂顺序为p35_h0_single_center、c156_single_center、p35_h0_rotating_control、c156_rotating_control；轮换仅为明确标出的冻结对照，两个新方案仍硬性只接收一个原中心。各方法标准化公式不变，统计仅重新拟合该臂的共同训练子集特征。相同网络参数/初始权重、样本顺序与逐轮随机排列配对；Dropout随机抽样因token数不同不承诺逐元素相同。

启动顺序：全量18文件和共同子集核验→四臂V100真实样本预检→4次V100训练并发上限4→独立预测复算。预算每臂最多500轮、早停50，Slurm时间上限6小时仅是资源上限，不改变训练停止规则。汇总检查全部8份预测的原行号、标签、原中心索引、0.5阈值、选轮、每流场与每实例召回、原测试/新增头部子集分数、参数和时间；核对所有重合epoch的样本排列。单种子成绩不提供标准差，不与全数据历史分数当作单因素比较。历史预检和失败记录保留。

本地五项检查覆盖同中心特征等价、中心缺失拒绝、单中心网络门禁及梯度、对照初始权重相同、四臂共同子集行号与真实中心一致。正式部署前通过；GPU工程小拟合不作为正式F1。

科学提交ad0ffc85已私有部署，52009985共同子集核验9秒及52009986四臂V100预检20秒均成功；52009987[0–3]正式训练和52009988复算已提交。启动检查确认同网络配对初始权重相同、同原中心特征误差0、固定中心输入[32,1,142]、原参数与批128反向通过。训练/测试保留全部Channel74/TBL58正类实例，测试每实例最少9/8束；验证有一个TBL实例失去正类行，保持用户选定的原集合和共同筛选。四次训练尚在Priority排队，无正式F1；证据outputs/Ablation_Task4C_OriginalCenter_1.1/formal_startup_verified.json。
<!-- task4c-original-center-subset-authorized-20260917-end -->

<!-- task4c-original-center-preflight-20260917 -->
2026-09-17原采样点单中心预检全部完成：科学commit2ef05ac0；52009638数据核验8秒、52009639 V100模型检查17秒、52009640_0/1两流场补积分诊断31/105秒，均COMPLETED/0:0。p35_h0/c156的原中心141维与旧实现中同中心特征逐值相同（最大误差0）；输入严格[32,1,142]，参数76,738/992,386、批128反向、跨批检查和无卷积检查通过。32真实训练样本小拟合F1均1.0，损失分别0.699520→0.000121、0.739039→0.000343；这些是工程检查，不是正式测试F1。

缺失原中心训练918、验证7、测试32全部按原参数补积分，0条通过旧清洗，957条全部是实际积分长度不足。故不能靠重新积分恢复原有效中心，也不能用邻居替代。已向用户更新必要选择：①共同原中心有效子集196,042/2,993/11,288，配对重跑两原方法与两单中心方法，以隔离中心策略；或②保留全部样本，但允许957条原中心使用真实较短积分长度。回复前不删除行、不放宽清洗、不正式训练；其他任务不恢复。先前询问的“补积分看看”已由只读诊断回答，现待上述数据处理选择。

证据`outputs/Ablation_Task4C_OriginalCenter_1.1/preflight_verified.json`、`gpu_check.json`、`original_center_audit.json`和`center_reintegration_{channel,tbl}.json`。已完成私有Ibex部署，公开GitHub发布未获自动审批，未进行公开推送；本地科学提交与原科学证据完整保留。正式单中心F1尚不存在。
<!-- task4c-original-center-preflight-20260917-end -->

2026-09-17用户紧急要求仅修改Task4-c p35/h0与c156的中心策略：一个三维采样点是一份样本；该点积分的线是唯一中心，其他线只作邻居。禁止轮换中心，也禁止使用最近有效线冒充原中心。本轮新授权覆盖此前此项暂停，未恢复其他Task3/5实验。

## 冻结范围

复用最新GT覆盖数据196,960训练/3,000验证/11,320测试，源科学commit c3952a7b及18个文件SHA256固定在配置。原标签、采样位置、划分、积分、旧文件和历史预测不修改。p35/h0指原Search2.2的32点、最近六邻居、neighbor_scale100/weight0.5、signed-log+训练集zscore+clip8、76,738参数；c156保留48点均匀弧长重采样、FPS六邻居、scale/weight1、直接训练集zscore、wide_residual992,386参数。两者六频率、141维、h0优化规则全部保留。

编码器一次只接收指定原中心及其六邻居，输出[B,1,142]（141特征＋有效标记），不计算其他中心的特征。为了只改中心策略，原MLP层、宽度、初始化、残差块、Dropout和分类头保持；原跨中心均值/最大值只接收一个向量，结果是[h,h]。没有通过复制中心token凑原线数，也没有平均多个中心。所有完整网络无卷积，前向钩子禁止非单token输入。归一化仍按原整束几何公式；训练特征统计仍只拟合当前训练特征。

固定一个共同种子96721，两个方法各完整训练一次，batch128、AdamW lr0.001/wd0.0001、Dropout0.15、clip5、最多500轮/早停50、验证loss平台15轮lr减半至1e-6；验证F1选轮次、平均精度打破平局、固定0.5阈值。先报告两次单种子结果，不作超参数搜索，不自动增加种子。单种子不提供跨种子方差。测试在每个模型选轮锁定后读取；报告完整测试、两流场、原10000束子集、新增头部子集与每GT召回。

## 原中心缺失：必须先解决的数据条件

只读Ibex检查发现原中心线缺失：Channel训练412/验证1/测试14；TBL训练506/验证6/测试18，合计918/7/32。保留中心的播种坐标误差小于6e-6个邻居间距，而缺失行最近线约相隔1个邻居间距，因此不是浮点匹配失败。原中心索引按metadata.center、centroid、radius与seeds核验；只接受1e-4个邻居间距内的原点，缺失明确为-1。原清洗只要求整束至少10条有效线，未要求中心线必须保留。

已向用户提出两种处理：补积分原采样点，仍不满足旧清洗则报告；或仅保留原中心尚在的共同子集并配对重跑旧方法。回复前正式训练门禁保持关闭，不静默丢行，不采用旧单中心试验的最近有效线替代。可独立执行只读补积分诊断，使用原ds/maxiteration/RK45/长度及退化清洗，记录每个缺失行是否可恢复，但不修改科学数据。最终处理决定必须补入本协议及配置后才能正式训练。

## 实施及预检

`FMT_Utils/Task4C_OriginalCenter_1_1.py`只实现原中心匹配、六邻居选择与单向量编码；`experiments/Task4C_OriginalCenter_1_1.py`复用冻结训练函数；配置`config/Ablation_Task4C_OriginalCenter_1.1.json`；启动器`ibex_bash/task4c_original_center_1p1.sh`。

本地三项测试验证同一原中心特征等价、缺失必须报错、单token/原参数/无卷积/梯度。首次测试仅因argsort stable参数调用形式失败，修正为显式dim后通过；不涉及科学公式改变。V100预检使用32个原中心保留的真实训练样本、批128反向、小拟合、跨批与原实现选定中心对照；这些是工程检查，不是正式F1。数据预检核对18文件哈希和全部原中心配额；完整代码包必须包含FLowUtils，避免另一暂停任务的部署依赖遗漏。

在缺失行处理回复之前仅提交verify-source、gpu-check及两流场diagnose-centers，共4个预检进程；没有正式训练成绩。预检日志、实际模块列表、参数和源哈希全部保留。

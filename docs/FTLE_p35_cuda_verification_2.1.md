# Verify_FTLEP35CUDA_2.1

> **2026-09-17撤销状态：** 本文涉及的Raw卷积＋FMT/残差或二维FMT卷积网络，已按用户要求从可用方案及有效FMT证据中删除；旧数值仅为撤销历史，不得用于当前主表、附录或“最佳FMT”结论。固定傅里叶特征本身及明确无卷积的Task1/Task4-c分支不受此撤销影响。以 [FMT禁止空间卷积协议1.1](FMT_no_spatial_convolution_protocol_1.1.md) 为准。


用途：开发CPU比较持续运行时，核对后续GPU执行是否保持同一训练输入语义，并检查严格确定性前后向是否可用。不是新增精度搜索，不访问validation/test的标签或路径文件。

每流场在4×首个训练切片的有效五线primitive上等距选择最多64个样本。CPU/CUDA特征逐项比较，进一步用仅训练集拟合的原标准化参数核对所有新表示。4×/8×的pyramid和U-Net融合及两冻结基线均执行随机张量前后向；检查已知节点保持和几何梯度。若CUDA不支持新U-Net的确定性插值反向，保留明确错误，U-Net可继续使用CPU，不更换算法来冒称复现。最终仅在被支持的结构上允许使用GPU。

一次GPU检查，调度允许可用NVIDIA设备并记录实际型号；严格保持冻结训练代码、种子规则和Fourier公式。模型只在内存，无checkpoint。实际结果和作业编号完成后登记；不同设备的最终比较仍要求每个流场/倍率/种子内所有方法共用同一进程和设备。

执行结果：51908968完成，科学commit `fbd89e090a1ed23544326b8f59cedd4af6179a4b`，实际设备NVIDIA GeForce GTX 1080 Ti。两种融合结构及原ESPCN/U-Net的4×/8×严格确定性前后向均PASS；训练样本编码经标准化后的CPU/GPU最大绝对差为1.0772404535237812e-09。没有访问验证或测试样本，没有新增精度拟合。证据为`outputs/Other_FTLEP35Fusion_2.1/cuda_verification.json`。

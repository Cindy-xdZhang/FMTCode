# Task4-c底部采样加密：Ablation_Task4C_BottomDensity_1.1

2026-09-15用户要求：停止当前剩余Conv3D，回到原约0.72对应的4.14数据，只增加底部各Hairpin实例区域采样密度，不增加积分长度变化；比较最佳FMT与8³/24³/32³ Conv3D。

## 唯一数据变化

直接读取科学commit5c9c9042生成的原4.14缓存，不重新随机划分。保留27,000训练、3,000验证、10,000测试全部原样本。按已向用户说明的默认加密方案，仅给原训练集中Channel62个、TBL54个有效Hairpin实例各追加100束正类训练样本；新增11,600束，共38,600训练、3,000验证、10,000测试。加密数量和仅训练加密已发出可选澄清，当前按默认执行，非额外标签授权。

原训练几何/种子/元数据作为新数组的逐值相同前缀，原验证/测试三个文件逐字节相同；全部原负样本不增不减。原测试是已使用的benchmark，不称新确认集。此前132,000束v2及历史成绩冻结。

新增中心仅取原正类头区、原训练空间块、对应同一个GT实例内部的单元；在每实例此单元池的z较低一半播种，中心须仍在同一GT实例内。沿原九种邻距/积分参数组合循环尝试，沿用原RK45追踪和清洗，成功接受至100束。失败不改变标签，不放宽清洗、空间距离或积分长度；若某实例无法达标则停止并报告。

保持原标签：完整候选头区GT单实例占比≥50%为正，零覆盖为负，混合排除；不采用局部种子新标签，也不补入旧标签规则排除的实例。没有新增完整实例留出。

新增训练中心到每个原验证/测试中心至少为该评价中心的一个局部网格间距；原评价到同头区训练中心不超过4h仍由保留的旧训练样本保证。原空间块与验证/测试之间的距离不变。

## 冻结的积分、模型与训练

Channel沿用(ds,maxiteration)=(0.001,80)/(0.002,50)/(0.004,30)，单向0.08/0.10/0.12；TBL=(0.025,160)/(0.05,100)/(0.04,150)，单向4/5/6。本次恢复用户指定的旧版数据参数，TBL6不套用后来v2的3–5.5范围。邻距0.25/0.5/1h，所有积分和原清洗代码不改；不使用v2新增的长度档位或额外全线长度筛选。

FMT固定p35/n0_k06：141维、6频率、最大半径归一化、训练有效线逐特征标准化、均值＋最大值池化、h0分类器，76,738参数。Conv沿用相同72,192参数小网络，分辨率仅8³/24³/32³；四通道体素、确定性池化和分类头均不变。

四方法各三种子96611–96613，共12次训练；V100、batch128、lr0.001、dropout0.15、weight decay0.0001、最多500轮，验证损失平台降lr，验证F1/AP选最佳轮次、50轮不改善早停。选择结束后测试一次，固定阈值0.5。数据构造和模型选择不依据本次test成绩修改。无checkpoint文件。

## 实现与验证

配置：config/Ablation_Task4C_BottomDensity_1.1.json。数据追加：FMT_Utils/Task4C_BottomDensity_1_1.py。入口：experiments/Task4C_BottomDensity_1_1.py。训练/编码直接复用冻结v2实现；用独立函数全局字典传入新运行身份，不修改旧模块。4.14源码、p35编码器、小Conv结构和旧运行配置保持原样。

依赖链：V100模型/体素检查→116实例每实例3束真实pilot→两流场正式追加→全量前缀/标签/评价文件哈希/距离/长度核验→编码→12次训练→逐样本预测复算及查看器数据包。共21调度进程，失败阻止后续；全部编号和状态记入docs/ibex_run_registry.md。

科学commit、真实预检与提交状态在运行登记补充；CPU四模型及三个体素分辨率检查已通过。未预设FMT优势；当前四模型比较不能单独回答分辨率和采样变化各自的因果效应。

experiment: Verify_Task3_ResidualHeadNormActivation_29.1
output_root: outputs/Verify_Task3_ResidualHeadNormActivation_29.1
task: Task3

# Development-only residual-head normalization x activation factorial.
# This grid was fixed without reading any partial or final 25.1--28.1 result.
# It independently freezes the completed 22.1 anchored FMT feature and the
# 5.2 development population/split. No confirmation population is opened.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0

combination_sources:
  feature:
    kind: stage1_feature
    selection: outputs/Verify_Task3_AnchoredFeatureDecomposition_22.1/stage1_selection.json
    expected_experiment: Verify_Task3_AnchoredFeatureDecomposition_22.1

# Complete 3 x 3 factorial. n00 is the exact historical deep-MLP control.
# Each cell is applied without change to both FMT and train-only Raw-PCA.
optimization_candidates:
  - {id: n00_control_layernorm_gelu, sources: [feature],
     model: {head_normalization: layernorm, head_activation: gelu}}
  - {id: n01_layernorm_silu, sources: [feature],
     model: {head_normalization: layernorm, head_activation: silu}}
  - {id: n02_layernorm_relu, sources: [feature],
     model: {head_normalization: layernorm, head_activation: relu}}
  - {id: n03_rmsnorm_gelu, sources: [feature],
     model: {head_normalization: rmsnorm, head_activation: gelu}}
  - {id: n04_rmsnorm_silu, sources: [feature],
     model: {head_normalization: rmsnorm, head_activation: silu}}
  - {id: n05_rmsnorm_relu, sources: [feature],
     model: {head_normalization: rmsnorm, head_activation: relu}}
  - {id: n06_none_gelu, sources: [feature],
     model: {head_normalization: none, head_activation: gelu}}
  - {id: n07_none_silu, sources: [feature],
     model: {head_normalization: none, head_activation: silu}}
  - {id: n08_none_relu, sources: [feature],
     model: {head_normalization: none, head_activation: relu}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: n00_control_layernorm_gelu
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.195
  target_absolute_fmt_f1: 0.893
  confirmation_opened: false

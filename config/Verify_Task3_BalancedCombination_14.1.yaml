experiment: Verify_Task3_BalancedCombination_14.1
output_root: outputs/Verify_Task3_BalancedCombination_14.1
task: Task3

# Development-only extension declared before either source selector is read.
# It asks one narrow question: does the frozen class-balanced sampler improve
# the already selected four-factor 11.1 recipe?  It avoids repeating the full
# 2^5 factorial and therefore needs only 20 array children / 120 paired runs.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0

combination_sources:
  core:
    selection: outputs/Verify_Task3_CombinedOptimization_11.1/optimization_selection.json
    expected_experiment: Verify_Task3_CombinedOptimization_11.1
  balanced:
    selection: outputs/Verify_Task3_ClassBalancedBatches_13.1/optimization_selection.json
    expected_experiment: Verify_Task3_ClassBalancedBatches_13.1

optimization_candidates:
  - {id: z00_core, sources: [core]}
  - {id: z01_core_balanced, sources: [core, balanced]}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_average_precision_gain_vs_raw_pca
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  target_dataset_macro_f1_gain: 0.165
  confirmation_opened: false

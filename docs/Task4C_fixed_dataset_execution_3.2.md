# Task4-c v3 正负比例修订执行记录

用户2026-09-19要求先停现有v3实验，再提高正样本采样概率，使最终正:负约2:1。科学版本`mainExp_Task4C_FixedDataset_3.2`，commit `4bf8ae3ca1b9128a4918efaaa34c647f864efc50`；私有目录 `/ibex/user/zhanx0o/FMT_Task4C_FixedDataset_3p2_20260919`。协议见[Task4C_fixed_dataset_protocol_3.2.md](Task4C_fixed_dataset_protocol_3.2.md)。

## 旧实验已停止

八个运行中的3.1训练均已CANCELLED，20:23 UTC再次核查队列为空；两组PointNet此前预检损失下降检查失败，训练从未启动。没有正式测试F1。已保留旧数据、共享邻居、缓存和日志，不自动恢复GPU实验。具体编号和证据见[3.1训练执行记录](Task4C_v3_training_execution_3.1.md)。

## 新构建已提交

| 阶段 | Slurm作业 | 资源 |
|---|---|---|
| 输入与单元测试核验 | 52111590 | 4 CPU / 16 GiB |
| Channel/TBL试构建与核验 | 52111591_0–1 | 各4 CPU / 48 GiB |
| 按试构建保留率校准概率 | 52111592 | 4 CPU / 4 GiB |
| 两流场正式重建 | 52111593_0–1 | 各4 CPU / 96 GiB |
| 全量数据审计 | 52111594 | 4 CPU / 96 GiB |

20:26 UTC：输入核验、两流场试构建与校准均COMPLETED / 0:0；两流场正式构建`52111593_0/1`均RUNNING，审计等待。试构建分别用时95/108秒，校准6秒。**尚未完成或冻结全量新数据**，最终比例须以全量审计为准。

试构建每场40,000点（正26,667 / 负13,333）；清洗后Channel正26,013 / 负12,604，比例2.063869；TBL正26,520 / 负13,238，比例2.003324。两场初始池与Poisson选点重建、包括异类点的全局距离、真实GT标签、原实例折映射及各500点重积分全部通过。

正式概率已经锁定在`calibration.json`：Channel正类概率0.6596533184461792（目标点正2,638,613 / 负1,361,387），TBL0.6663059018640085（正2,665,224 / 负1,334,776）。这里是清洗前配额；因为正类实测保留率稍高，概率略低于2/3，以使清洗后更接近2:1。不得将试构建比例当作正式800万目标构建的最终比例。

仍为M=40,000,000、目标N=8,000,000，每场各半；固定一套随机数。试构建先以2/3正样本配额生成，校准两类积分清洗保留率后调整正式配额；最终每流场要求1.9–2.1的正负比。候选、积分、清洗、GT标签、归属、实例→折映射不改；不补采、不复制点、不训练。

## 已完成的验证

本地21项测试通过；部署远端21项测试再次通过（2.877秒），Slurm输入核验再次通过21项（1.594秒）。新配额Poisson与独立全对距离参考一致，包括异类点距离、边界和重复位置；两类条件体积采样及概率修正测试通过。

部署包SHA-256 `e9aea8a447fed9624a150e701b636df603128efb5b4bb94f231202b745371a49`；23份科学源码/配置/测试文件哈希与固定Git提交逐一相同。提交前磁盘可用658 GiB。没有重新提交或增加种子。

本地证据目录 `outputs/mainExp_Task4C_FixedDataset_3.2/remote_evidence/`；正式使用前须核查`data_audit.json complete=true`和哈希。输入核验通过或试构建完成都不等于全量数据完成。

experiment: Verify_Task3_ResidualHeadDepthWidth_31.1
output_root: outputs/Verify_Task3_ResidualHeadDepthWidth_31.1
task: Task3

# Development-only residual-head capacity factorial. This experiment was
# designed after 25.1 and 26.1 completed: both enlarged the paired gap while
# reducing absolute FMT F1. Therefore selection below applies a zero-tolerance
# absolute FMT guard. No confirmation population is opened.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  feature:
    kind: stage1_feature
    selection: outputs/Verify_Task3_AnchoredFeatureDecomposition_22.1/stage1_selection.json
    expected_experiment: Verify_Task3_AnchoredFeatureDecomposition_22.1

# Complete 5 widths x 3 depths factorial. c00 is the exact historical
# two-hidden-layer, width-64 deep MLP. Each cell is applied unchanged to FMT
# and train-only Raw-PCA; preflight rejects any cell above the Raw-wide cap.
optimization_candidates:
  - {id: c00_control_w64_d2, sources: [feature],
     model: {head_hidden_dim: 64, head_depth: 2}}
  - {id: c01_w32_d1, sources: [feature],
     model: {head_hidden_dim: 32, head_depth: 1}}
  - {id: c02_w32_d2, sources: [feature],
     model: {head_hidden_dim: 32, head_depth: 2}}
  - {id: c03_w32_d3, sources: [feature],
     model: {head_hidden_dim: 32, head_depth: 3}}
  - {id: c04_w48_d1, sources: [feature],
     model: {head_hidden_dim: 48, head_depth: 1}}
  - {id: c05_w48_d2, sources: [feature],
     model: {head_hidden_dim: 48, head_depth: 2}}
  - {id: c06_w48_d3, sources: [feature],
     model: {head_hidden_dim: 48, head_depth: 3}}
  - {id: c07_w64_d1, sources: [feature],
     model: {head_hidden_dim: 64, head_depth: 1}}
  - {id: c08_w64_d3, sources: [feature],
     model: {head_hidden_dim: 64, head_depth: 3}}
  - {id: c09_w80_d1, sources: [feature],
     model: {head_hidden_dim: 80, head_depth: 1}}
  - {id: c10_w80_d2, sources: [feature],
     model: {head_hidden_dim: 80, head_depth: 2}}
  - {id: c11_w80_d3, sources: [feature],
     model: {head_hidden_dim: 80, head_depth: 3}}
  - {id: c12_w96_d1, sources: [feature],
     model: {head_hidden_dim: 96, head_depth: 1}}
  - {id: c13_w96_d2, sources: [feature],
     model: {head_hidden_dim: 96, head_depth: 2}}
  - {id: c14_w96_d3, sources: [feature],
     model: {head_hidden_dim: 96, head_depth: 3}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: c00_control_w64_d2
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.195
  target_absolute_fmt_f1: 0.893
  confirmation_opened: false

experiment: Verify_Task3_ResidualHeadEpsilonRefresh_109.1
output_root: outputs/Verify_Task3_ResidualHeadEpsilonRefresh_109.1
task: Task3

# Frozen while 77.1 remains incomplete and before 107.1 can produce any
# performance metric. The independently audited 108.1 portfolio supplies one
# complete recipe per physical family. The exact source is the control; the
# remaining candidates change only downstream residual-head Adam epsilon.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_ResidualHeadBeta1RefreshPortfolio_108.1/portfolio_selection.json
    expected_experiment: Verify_Task3_ResidualHeadBeta1RefreshPortfolio_108.1

allowed_source_overrides: [training.residual_head_optimizer_epsilon]

# e00 preserves the complete 108.1 source recipe. Each remaining cell replaces
# only epsilon for trainable parameters outside fmt_encoder. Both head betas,
# all auxiliary optimizer settings, and the frozen Raw backbone are unchanged.
optimization_candidates:
  - {id: e00_control_source, sources: [portfolio]}
  - {id: e01_headeps1e12, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-12}}
  - {id: e02_headeps1e10, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-10}}
  - {id: e03_headeps1e9, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-9}}
  - {id: e04_headeps1e8, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-8}}
  - {id: e05_headeps1e7, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-7}}
  - {id: e06_headeps1e6, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-6}}
  - {id: e07_headeps1e5, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-5}}
  - {id: e08_headeps1e4, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-4}}
  - {id: e09_headeps1e3, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-3}}
  - {id: e10_headeps1e2, sources: [portfolio], training: {residual_head_optimizer_epsilon: 1.0e-2}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: e00_control_source
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.248
  target_absolute_fmt_f1: 0.907
  confirmation_opened: false

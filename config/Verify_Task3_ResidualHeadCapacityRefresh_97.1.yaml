experiment: Verify_Task3_ResidualHeadCapacityRefresh_97.1
output_root: outputs/Verify_Task3_ResidualHeadCapacityRefresh_97.1
task: Task3

# Frozen while 77.1 remains incomplete and before 96.1 can produce any
# portfolio metric. The independently audited 96.1 portfolio supplies one
# complete recipe per physical family. The exact source is the control; the
# remaining candidates replace only residual-head hidden width and depth.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_AuxiliaryActivationResidualGainPortfolio_96.1/portfolio_selection.json
    expected_experiment: Verify_Task3_AuxiliaryActivationResidualGainPortfolio_96.1

allowed_source_overrides: [model.head_hidden_dim, model.head_depth]

# h00 is the complete 96.1 source recipe. The 12 remaining rows are a complete
# capacity-safe 4-width x 3-depth factorial previously validated by 31.2, now
# repeated after the substantially newer auxiliary-projection portfolio.
optimization_candidates:
  - {id: h00_control_source, sources: [portfolio]}
  - {id: h01_w32_d1, sources: [portfolio], model: {head_hidden_dim: 32, head_depth: 1}}
  - {id: h02_w32_d2, sources: [portfolio], model: {head_hidden_dim: 32, head_depth: 2}}
  - {id: h03_w32_d3, sources: [portfolio], model: {head_hidden_dim: 32, head_depth: 3}}
  - {id: h04_w48_d1, sources: [portfolio], model: {head_hidden_dim: 48, head_depth: 1}}
  - {id: h05_w48_d2, sources: [portfolio], model: {head_hidden_dim: 48, head_depth: 2}}
  - {id: h06_w48_d3, sources: [portfolio], model: {head_hidden_dim: 48, head_depth: 3}}
  - {id: h07_w64_d1, sources: [portfolio], model: {head_hidden_dim: 64, head_depth: 1}}
  - {id: h08_w64_d2, sources: [portfolio], model: {head_hidden_dim: 64, head_depth: 2}}
  - {id: h09_w64_d3, sources: [portfolio], model: {head_hidden_dim: 64, head_depth: 3}}
  - {id: h10_w80_d1, sources: [portfolio], model: {head_hidden_dim: 80, head_depth: 1}}
  - {id: h11_w80_d2, sources: [portfolio], model: {head_hidden_dim: 80, head_depth: 2}}
  - {id: h12_w80_d3, sources: [portfolio], model: {head_hidden_dim: 80, head_depth: 3}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: h00_control_source
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.236
  target_absolute_fmt_f1: 0.901
  confirmation_opened: false

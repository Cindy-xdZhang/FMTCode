experiment: Verify_Task3_AuxiliaryProjectionActivation_87.1
output_root: outputs/Verify_Task3_AuxiliaryProjectionActivation_87.1
task: Task3

# Development-only single-factor search frozen while 77.1 remains incomplete,
# before any 86.1 portfolio metric exists. The independently audited 86.1
# portfolio supplies one complete recipe per physical family. Candidates only
# replace activation modules already present inside fmt_encoder.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_AuxiliaryLinearBiasScalePortfolio_86.1/portfolio_selection.json
    expected_experiment: Verify_Task3_AuxiliaryLinearBiasScalePortfolio_86.1

allowed_source_overrides: [model.auxiliary_projection_activation_override]

# a00 preserves every source module exactly. Other cells replace every
# existing activation in the paired auxiliary projection, without adding
# layers or parameters. Linear weights, biases, normalization and RNG state
# remain fixed.
optimization_candidates:
  - {id: a00_control_source, sources: [portfolio]}
  - {id: a01_identity, sources: [portfolio], model: {auxiliary_projection_activation_override: identity}}
  - {id: a02_silu, sources: [portfolio], model: {auxiliary_projection_activation_override: silu}}
  - {id: a03_relu, sources: [portfolio], model: {auxiliary_projection_activation_override: relu}}
  - {id: a04_leakyrelu01, sources: [portfolio], model: {auxiliary_projection_activation_override: leaky_relu_01}}
  - {id: a05_elu, sources: [portfolio], model: {auxiliary_projection_activation_override: elu}}
  - {id: a06_mish, sources: [portfolio], model: {auxiliary_projection_activation_override: mish}}
  - {id: a07_tanh, sources: [portfolio], model: {auxiliary_projection_activation_override: tanh}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: a00_control_source
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.226
  target_absolute_fmt_f1: 0.896
  confirmation_opened: false

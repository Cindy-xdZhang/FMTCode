experiment: Verify_Task3_ResidualHeadBeta2Refresh_105.1
output_root: outputs/Verify_Task3_ResidualHeadBeta2Refresh_105.1
task: Task3

# Frozen while 77.1 remains incomplete and before 103.1 can produce any
# performance metric. The independently audited 104.1 portfolio supplies one
# complete recipe per physical family. The exact source is the control; the
# remaining candidates change only downstream residual-head Adam beta2.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_ResidualHeadWeightDecayRefreshPortfolio_104.1/portfolio_selection.json
    expected_experiment: Verify_Task3_ResidualHeadWeightDecayRefreshPortfolio_104.1

allowed_source_overrides: [training.residual_head_optimizer_beta2]

# b00 preserves the complete 104.1 source recipe. Each remaining cell replaces
# only beta2 for trainable parameters outside fmt_encoder. The head inherits
# source beta1; the auxiliary projection keeps its source betas and other
# optimizer settings; the frozen Raw backbone remains excluded.
optimization_candidates:
  - {id: b00_control_source, sources: [portfolio]}
  - {id: b01_headbeta2_0000, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.0}}
  - {id: b02_headbeta2_0500, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.5}}
  - {id: b03_headbeta2_0800, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.8}}
  - {id: b04_headbeta2_0900, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.9}}
  - {id: b05_headbeta2_0950, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.95}}
  - {id: b06_headbeta2_0980, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.98}}
  - {id: b07_headbeta2_0990, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.99}}
  - {id: b08_headbeta2_0995, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.995}}
  - {id: b09_headbeta2_0999, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.999}}
  - {id: b10_headbeta2_09999, sources: [portfolio], training: {residual_head_optimizer_beta2: 0.9999}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: b00_control_source
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.244
  target_absolute_fmt_f1: 0.905
  confirmation_opened: false

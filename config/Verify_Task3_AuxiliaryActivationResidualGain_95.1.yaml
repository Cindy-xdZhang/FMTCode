experiment: Verify_Task3_AuxiliaryActivationResidualGain_95.1
output_root: outputs/Verify_Task3_AuxiliaryActivationResidualGain_95.1
task: Task3

# Frozen while 77.1 remains incomplete and before 94.1 can produce any
# portfolio metric. The independently audited 94.1 portfolio supplies one
# complete recipe per physical family. Candidates retain the complete current
# activation response and add a fixed linear bypass g*x after any 93.1 mix.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_AuxiliaryActivationResidualMixPortfolio_94.1/portfolio_selection.json
    expected_experiment: Verify_Task3_AuxiliaryActivationResidualMixPortfolio_94.1

allowed_source_overrides: [model.auxiliary_projection_activation_residual_gain]

# g00 preserves every source operation exactly. For g>0 each complete current
# activation response f(x) is replaced by f(x)+g*x without parameters/buffers.
optimization_candidates:
  - {id: g00_control_source, sources: [portfolio]}
  - {id: g01_gain001, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 0.01}}
  - {id: g02_gain003, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 0.03}}
  - {id: g03_gain010, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 0.1}}
  - {id: g04_gain025, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 0.25}}
  - {id: g05_gain050, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 0.5}}
  - {id: g06_gain100, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 1.0}}
  - {id: g07_gain200, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 2.0}}
  - {id: g08_gain400, sources: [portfolio], model: {auxiliary_projection_activation_residual_gain: 4.0}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: g00_control_source
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.234
  target_absolute_fmt_f1: 0.900
  confirmation_opened: false

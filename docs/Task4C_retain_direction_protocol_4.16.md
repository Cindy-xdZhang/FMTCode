# Task4-c 4.16：保留4.14完整几何输入再加入角度

2026-09-14用户指出4.15偏离“给原4.14加入角度”的要求。本版为`Verify_Task4C_RetainDirection_4.16`，纠正移除72维坐标/切向方向频谱的问题。4.14、4.15源码、数据和成绩冻结；方法结论只写入experiment_log。

## 固定比较

| 方法标识 | 实际输入，均不含末尾mask | 参数量 |
|---|---|---:|
| fmt_4_14 | 原4.14缓存：局部FMT161＋坐标/切向频谱72，共233维 | 88,514 |
| fmt_v5_4_14 | 原233维逐位复制＋fmt_v5 5.1的角度165维，共398维 | 109,634 |
| fmt_objective_ntod_v2_4_14 | 2.2距离231维＋原72维逐位复制＋角度165维，共468维 | 118,594 |
| Conv3D | 复用4.14原24³四通道体素结果，三个种子在4.15已完全复现 | 219,602 |

72维来自曲线坐标和单位切向的实部/虚部频谱，是合法的几何输入，不是kin4/kin6或IVD估计。对fmt_v5的比较只增加角度；距离＋角度2.2版替换局部编码器，并保留原附加输入，不将后者比较解释为单独角度增益。距离/角度标量块具有固定对应关系下的不变性，但包含72维方向信息的整套模型不称为完全客观。

Task4-c曲线是静态快照沿涡量场积分的涡线。32个点的索引代表归一化弧长，不代表物理时间。中心线及其六条最近播种邻居、6个傅里叶频率、15个中心夹角及165维角度系数全部复用已有5.1/2.2实现。无新增运动学/IVD输入、重采样或丢弃样本。

## 数据、网络和训练冻结

父版本`mainExp_Task4C_PhysicalLength_4.14`；科学commit `5c9c904228d5185447a084953b2652a8938c0485`。配置SHA256 `3909eaab4093c0cb85f9bbd400fcf81e9dc036c879df7b8b9f6f214b6a281e6d`；summary SHA256 `7104bcbf34e637a6462c5eb77936e7b37f6731f2bd13368d0eed45b9d82b9889`。原始几何、播种点、标签、编码缓存逐文件核验哈希。原233维直接读旧缓存，不重算。

两流场各13,500训练、1,500验证、5,000测试，合计27,000/3,000/10,000。共享头区/实例/源快照，仍是已使用的4.14局部空间benchmark，不是新确认集或独立涡实例泛化。

直接调用冻结`Task4C_NearbySampling_4_13.py::train`。三种子96611/96612/96613，dropout0.15、weight decay0.0001；AdamW学习率0.001，batch128，最多500epoch，学习率平台耐心15、早停50。普通交叉熵，每epoch全训练集一次。输入signed-log、训练有效线逐通道均值/标准差、截断±8；验证F1固定0.5阈值选模型，同分按Average Precision（平均精确率，按召回率增量加权的精确率）选择。选择锁后测试一次，不作搜索。

原模型实例化及全部权重初始化完全保持。扩展模型使用4.15既有适配器：首层输入宽度随特征增加，完整首层按PyTorch Linear默认重新初始化；同种子其余隐藏层初始值完全相同。不宣称全部初始参数逐位相同或参数量相同，也不引入新的初始化方法。每个模型仅运行预定三个种子。

## 核验与记录

入口`experiments/Task4C_RetainDirection_4_16.py`；适配器`FMT_Utils/Task4C_RetainDirection_4_16.py`；核验`experiments/Audit_Task4C_RetainDirection_4_16.py`；配置`config/Verify_Task4C_RetainDirection_4.16.json`。

预检仅取每流场64条训练样本，检查完整233维保留、两新版165维角度与独立编码一致、原72维保留、mask和初始化；运行各两epoch工程试验。正式九次训练后，独立复算全部预测及样本身份、模型选择、每轮训练覆盖；检查全40,000条数据原特征逐位相同、标准化233维完全一致，并要求原FMT三种子训练/验证/测试F1复现4.14。Conv3D只读原结果和预测独立复算，明确标为复用参照。

Ibex所有进程登记提交/开始/结束、源码和配置哈希、节点/GPU、失败记录；无checkpoint文件。只在个人Ibex部署，本地commit确定源码，不向公开GitHub推送。结果无论高低均记录，不按已看到的测试成绩改参数。

## 完成及原设备复核记录

4.16科学commit `dfdc87b10f0d3de21d694e20f3dc97f7295dc66f`，配置SHA256 `660f8582c63a2d17ef332596685cec3249b59ba270e5ecd40ecedd1258e7faf1`；单个原设备复核4.17科学commit `e5de2877ca756973116d6dd2bfc8254c16d4ac4f`，配置SHA256 `6083ddbefd98e413ee4d7326df42b42d585cff9db73a0a0d01644b66f572d6f3`。最终有效对比`outputs/Verify_Task4C_BaselineDevice_4.17/validated_comparison.json`，SHA256 `c782f1f879d3e719d04fb77a70d2f230126edfe2d5299dda37f587fffab70968`。

4.16预检51887792、编码51887818、九次训练51887819[0-8]、汇总51887822完成；指标独立复算最大差异0。原核验51887823因检测到A100基线的归一化差异失败，失败和A100结果保留。4.17仅以V100重跑原FMT seed96611，job51888313完成；原233维归一化、验证选定epoch及训练/验证/测试F1完全复现。其余两个原FMT种子原已完全复现，两个新方法全部六次均为预定V100运行，未重跑/挑选新方法。最终检查覆盖全部40,000条特征保留、原通道标准化、预测、原Conv3D参照；14个调度进程（13成功、1保留失败）、28条生命周期记录核对PASS，无权重文件。运行审计代码commit7302a46b。

本地证据在`outputs/Verify_Task4C_RetainDirection_4.16`（124文件）及`outputs/Verify_Task4C_BaselineDevice_4.17`（15文件）。无模型证据包SHA256分别`7ce3ba4faf10ba266f5f44cc566c5aace86229f9e6bea6dc92b1399243b7454d`、`12f550fae5fce136e7c068f0cde88d0827ebc1a2ef99deb194b42dc270c8737b`。个人Ibex对应根目录为`/ibex/user/zhanx0o/FMT_Task4C_RetainDirection_20260914`及`/ibex/user/zhanx0o/FMT_Task4C_BaselineDevice_20260914`。

成绩及方法结论见experiment_log的task4c-retain-direction-4-16-2026-09-14。

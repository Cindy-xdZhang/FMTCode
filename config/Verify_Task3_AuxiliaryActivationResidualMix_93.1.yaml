experiment: Verify_Task3_AuxiliaryActivationResidualMix_93.1
output_root: outputs/Verify_Task3_AuxiliaryActivationResidualMix_93.1
task: Task3

# Frozen while 77.1 remains incomplete and before 92.1 can produce any
# portfolio metric. The independently audited 92.1 portfolio supplies one
# complete recipe per physical family. Candidates mix each existing activation
# output with the same pre-activation value after any input scale and shift.
base_search_config: config/Verify_Task3_SpatialRobust_5.2.yaml
base_search_config_sha256: c942fec55d841274c6850bc147d450c03b389609c130aa11bc4620b74361294f
upstream_selection: outputs/Verify_Task3_SpatialRobust_5.2/stage2_selection.json
upstream_selector_job_id: 50958095

paired_seeds: [40, 41, 42]

model_override:
  head_architecture: deep_mlp
  head_hidden_dim: 64
  head_depth: 2
  head_dropout: 0.0
  head_normalization: layernorm
  head_activation: gelu

combination_sources:
  portfolio:
    kind: optimization
    selection: outputs/Verify_Task3_AuxiliaryActivationInputShiftPortfolio_92.1/portfolio_selection.json
    expected_experiment: Verify_Task3_AuxiliaryActivationInputShiftPortfolio_92.1

allowed_source_overrides: [model.auxiliary_projection_activation_residual_mix]

# r00 preserves every source operation exactly. For r>0 each activation is
# replaced by (1-r)*activation(x)+r*x without parameters or buffers.
optimization_candidates:
  - {id: r00_control_source, sources: [portfolio]}
  - {id: r01_mix005, sources: [portfolio], model: {auxiliary_projection_activation_residual_mix: 0.05}}
  - {id: r02_mix010, sources: [portfolio], model: {auxiliary_projection_activation_residual_mix: 0.1}}
  - {id: r03_mix025, sources: [portfolio], model: {auxiliary_projection_activation_residual_mix: 0.25}}
  - {id: r04_mix050, sources: [portfolio], model: {auxiliary_projection_activation_residual_mix: 0.5}}
  - {id: r05_mix075, sources: [portfolio], model: {auxiliary_projection_activation_residual_mix: 0.75}}
  - {id: r06_mix090, sources: [portfolio], model: {auxiliary_projection_activation_residual_mix: 0.9}}
  - {id: r07_mix100, sources: [portfolio], model: {auxiliary_projection_activation_residual_mix: 1.0}}

selection:
  primary_metric: dataset_macro_f1_gain_vs_raw_pca
  tie_breakers:
    - dataset_macro_fmt_f1
    - dataset_macro_average_precision_gain_vs_raw_pca
    - dataset_macro_fmt_average_precision
    - positive_dataset_count
    - worst_dataset_f1_gain
    - worst_seed_f1_gain
  absolute_fmt_guard:
    control_optimization_id: r00_control_source
    f1_tolerance: 0.0
    average_precision_tolerance: 0.0
  target_dataset_macro_f1_gain: 0.232
  target_absolute_fmt_f1: 0.899
  confirmation_opened: false

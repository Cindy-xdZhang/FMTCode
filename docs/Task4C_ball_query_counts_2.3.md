# Verify_Task4C_BallQueryCounts_2.3

2026-09-19 用户重新指定固定 h：Channel 使用 Δy = **0.00613582134**，TBL 使用 Δz = **0.00723510003**。严格使用这两个十进制常数；原始 VTK 相应轴最小相邻间距分别为 0.006135821342468262 和 0.007235100027173758，差异只来自用户提供数值的舍入。Channel 的旧全局最小间距 0.000016510486602783203 来自 z 轴，已不再作为新查询的 h。旧统计和已取消训练保持归档。

## 数据与计数

沿用冻结数据 v2 r3，Channel 189,416 / TBL 198,413 个样本；审计 SHA-256 `e5aaa4fb8a8e01882c461da5916b3bb96883e2787b231b018b512d7e30d3a2a8`。不重建数据、不改标签或划分。

球心为播种点，物理半径为 `radius_h * h`。邻居池是同流场全部样本，计数排除自身、包含边界。每个邻居在给定目标积分长度下提供一条曲线，不能把三种长度重复计成三个邻居。界面筛选训练/验证/测试或正负标签时，只筛选球心，邻居池不变。

本次是固定球的原始数量分布，不扩大半径、不执行最远点采样。半径倍数为 0.25、0.5、0.75、1、1.25、1.5、2、2.5、3、4、5、6、8、10、12、16、24、32、48、64、128、256、512、1024。

## 结果

| 流场 | 半径 | 最少 | 平均 | 最多 | 不足16个的样本数 |
|---|---:|---:|---:|---:|---:|
| Channel | 1h | 0 | 1.793111 | 8 | 189416 |
| Channel | 3h | 0 | 38.055497 | 105 | 21023 |
| Channel | 4h | 0 | 66.291961 | 221 | 9017 |
| TBL | 1h | 0 | 0 | 0 | 198413 |
| TBL | 48h | 0 | 29.528216 | 81 | 33200 |
| TBL | 64h | 0 | 53.895198 | 177 | 12776 |
| Channel | 1024h | 189415 | 189415 | 189415 | 0 |
| TBL | 1024h | 566 | 18763.082127 | 31984 | 0 |

旧 h 下 Channel 的 1024h 半径约 0.0169067，新 h 下变为约 6.28308；旧平均31.817个邻居与新平均189415个邻居对应不同物理球，不可混用。仅改 h 的计量定义也不能保证两流场在相同倍数下具有相同采样密度。

## 实现与核验

- 配置：`config/Verify_Task4C_BallQueryCounts_2.3.json`。
- 全量统计：`experiments/Analyze_Task4C_BallQuery_2_3.py`；cKDTree 只返回计数，不把大球中的全部邻居列表存入内存。
- 双流场交互图：`experiments/Build_Task4C_BallQueryViewer_2_3.py`、`experiments/templates/task4c_ball_query_counts_2_3.html`。共享直方图区间和纵轴百分比；显示物理半径、最少/平均/中位数/最多、零邻居、少于6/16个比例；大数量区间扩展至262144。
- 证据：`outputs/Verify_Task4C_BallQueryCounts_2.3/radius_statistics.json`、`counts_channel.npz`、`counts_tbl.npz`、`h_channel.npy`、`h_tbl.npy`、`viewer/manifest.json`。统计 JSON SHA-256 `7ec742a15eeb008d6a5c269ac113af47bae5847cd401c6f82db6d8b002d37b51`。
- 核验原始 VTK 文件哈希、冻结数据文件哈希、每点各半径计数单调性与上下界；另对 Channel 62 / TBL 50 个球心（固定随机样本与各半径极值点），逐个直接扫描全部播种点计算欧氏距离，复核全部24档半径，全部通过。直方图各组总数等于所选球心数。

本次只完成统计与可视化，没有提交新 F1 实验。此前旧 h 的1024h重跑尚未提交；用户此次先要求重看分布，不将旧倍数自动移用到新 h。旧1h四臂仍为已取消，后续实验继续遵守单种子要求。

| 版本 | 定义与代码 | 当前状态 |
|---|---|---|
| Verify_Task4C_BallQueryCounts_2.2 | 全局最小 x/y/z 间距；Analyze_Task4C_BallQuery_2_2.py | 原统计归档 |
| Verify_Task4C_BallQueryCounts_2.3 | 用户固定 Channel Δy / TBL Δz；Analyze_Task4C_BallQuery_2_3.py | 全量统计和独立距离核验通过；新可视化已生成 |

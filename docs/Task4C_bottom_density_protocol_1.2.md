# Task4-c训练集扩大五倍：Ablation_Task4C_BottomDensity_1.2

2026-09-16用户要求：沿用当前数据构造方式，只将训练集扩大五倍，保留下部Hairpin区域较高的采样概率；比较最佳FMT、16³和24³ Conv3D。

## 数据

来源是科学commit `99cb6438bf11862d2cb333f3b1ff3eddc7d7b767` 的 `Ablation_Task4C_BottomDensity_1.1` 缓存。保留全部38,600束原训练样本；每个原训练样本对应的候选头区、类别、实例、尺度组合及完整/下半区采样池各新增四个播种中心，重新执行RK45积分和原清洗。原几何不复制为新增样本，新增中心不等于任何原训练中心，新增中心之间也不重复。

|流场|原训练|新增训练|新训练|新训练负/正类|验证|测试|
|---|---:|---:|---:|---:|---:|---:|
|Channel|19,700|78,800|98,500|54,090 / 44,410|1,500|5,000|
|TBL|18,900|75,600|94,500|57,815 / 36,685|1,500|5,000|
|合计|38,600|154,400|193,000|111,905 / 81,095|3,000|10,000|

原38,600束训练几何、播种点和元数据作为完全相同的前缀。验证/测试直接复制，三个文件逐字节相同；不重新划分、不按结果调整评价样本。原测试是已使用的benchmark。

下部偏置沿用1.1：原13,500束/流场使用完整候选头区训练单元池；1.1追加的Hairpin样本按其原中心轮次使用完整池或该实例原正类候选训练单元中z较低的一半。对每个来源样本沿用同一个池，四次重新播种。因此流场、正负类别、候选头区、实例、尺度和完整/下半区采样池的样本数均恰好为原来的五倍。具体新中心的高度分布会有随机波动。配置中的 `density` 是冻结的1.1来源定义，新增数量和规则由 `expansion` 定义。

新增中心在对应候选头区的原训练空间块中重新均匀选单元、在单元内播种；不要求靠近某个原训练中心。到任一原验证/测试中心的距离仍至少为该评价中心的一个局部网格间距。没有新增完整实例留出。标签仍为完整候选头区GT单实例占比≥50%为正、零覆盖为负、混合排除。不得加入局部种子新标签、GT内部硬裁剪或新负样本规则。

Channel保持原(ds,maxiteration)=(0.001,80)/(0.002,50)/(0.004,30)，单向0.08/0.10/0.12；TBL保持(0.025,160)/(0.05,100)/(0.04,150)，单向4/5/6。这里沿用当前1.1的旧4.14长度，不加入后来v2的长度变化。邻距仍为0.25/0.5/1h，RK45、至少10条有效线、最多27线、每线32点及质心/最大半径归一化均不变。

## 模型和训练

FMT固定p35/n0_k06：每线141维、6频率、最大半径归一化、训练集有效线逐特征标准化、逐特征均值＋最大值池化、h0分类器。可训练参数76,738；固定FMT编码器无可训练参数。

Conv3D只运行16³、24³：同样的占据率＋三维切向四通道体素输入、12/24/48卷积通道及384→70→64→2分类头。两分辨率都是72,192参数，即FMT的94.08%。结构、确定性池化及稀疏体素缓存实现不变。

各三个种子96611–96613，共九次训练。V100、batch128、lr0.001、dropout0.15、weight decay0.0001、最多500轮；验证损失平台降学习率，验证F1/AP选择最佳轮次，50轮不改善早停。每轮仍遍历所有训练样本一次，因此每轮优化步数随样本量扩大约五倍；不宣称计算预算相同。FMT标准化只从新训练集重新估计。选定轮次后读取测试，固定阈值0.5，无checkpoint文件。

## 实现和核验

- 配置：`config/Ablation_Task4C_BottomDensity_1.2.json`。
- 数据：`FMT_Utils/Task4C_BottomDensity_1_2.py`。
- 入口：`experiments/Task4C_BottomDensity_1_2.py`；Slurm脚本：`ibex_bash/task4c_bottom_density_1p2.sh`。
- 冻结复用1.1/4.14的候选区、标签、追踪、清洗和v2的编码/训练函数，不修改历史源码。

先执行16³/24³ CPU及V100体素往返、独立体素参考、网络前后向和参数核验，再对每个候选头区×尺度×采样池做一个真实新中心pilot；pilot仅检查几何与存储，不训练或选择方法。存储预检使用两倍pilot估算并预留10 GiB，不清除已有结果。

依赖链为V100预检→两流场pilot→容量检查→两流场正式生成→全量核验→两流场编码→九次训练→预测复算与汇总，共19个调度进程。全量核验检查原数组前缀、评价文件哈希、每来源恰好四束新几何、所有采样类别和尺度比例、新中心唯一性、训练空间块、清洗/半线弧长/步数、归一化及评价距离。任一步失败阻止后续，不能通过改标签、长度或距离规则补足。

CPU模型及体素核验已通过。科学commit、Ibex编号及实测数据核验记录在 `docs/ibex_run_registry.md`；方法结论只在 `docs/experiment_log.md` 记录。目前没有新F1结果。

科学commit `a7643890d22c7faf63c1f716bb6222ba5271c5ff` 已公开推送；Ibex51920910–51920917共19进程已提交。V100检查、Channel6,636/TBL9,717采样组合的真实pilot及容量检查通过，正式154,400束新增训练数据生成已启动。暂未取得完整新数据核验和新F1；不将已提交训练写成完成。原始启动证据为outputs/Ablation_Task4C_BottomDensity_1.2/startup_evidence.json。

2026-09-16最终完成：193,000/3,000/10,000及全部来源前缀/评价文件/新中心/五倍比例核验通过；九次训练和27预测、19进程38事件独立核对通过，无模型文件。科学commita7643890，最终时间03:44:38 UTC；成绩和方法结论见experiment_log的task4c-bottom-density-fivefold-final-20260916，表见paper_tables_tasks_3d。旧启动快照保留，本条为最终状态。
